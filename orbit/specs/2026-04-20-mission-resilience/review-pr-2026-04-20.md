# Pre-Merge Review

**Date:** 2026-04-20
**Reviewer:** Context-separated agent (fresh session)
**Branch:** rally/mission-resilience
**Spec:** orbit/specs/2026-04-20-mission-resilience/spec.yaml (v1.2)
**Verdict:** APPROVE

---

## Scope

The rally branch stacks five commits off `main` (approved-design commit is part of the base per the caller's brief). The implementation commit is `0897c45 feat(mission-resilience): three-layer spec fidelity through disruptions`. The diff against `main` touches:

```
 orbit/cards/0009-mission-resilience.yaml           |   3 +-
 orbit/specs/2026-04-20-mission-resilience/drive.yaml |  14 ++
 orbit/specs/2026-04-20-mission-resilience/progress.md |  59 ++++
 orbit/specs/2026-04-20-mission-resilience/review-spec-2026-04-20-v2.md | 55 ++
 orbit/specs/2026-04-20-mission-resilience/review-spec-2026-04-20-v3.md | 40 ++
 orbit/specs/2026-04-20-mission-resilience/review-spec-2026-04-20.md    | 82 ++
 orbit/specs/2026-04-20-mission-resilience/spec.yaml                   | 158 ++
 plugins/orb/scripts/session-context.sh             |  79 ++
 plugins/orb/skills/implement/SKILL.md              |  58 +/-
 plugins/orb/skills/review-spec/SKILL.md            |   8 +/-
```

The three-layer product surfaces are:

1. **State** — `progress.md` template in `plugins/orb/skills/implement/SKILL.md` §4
2. **Surface** — `plugins/orb/scripts/session-context.sh` additions (lines 200–280)
3. **Behaviour** — `plugins/orb/skills/implement/SKILL.md` §§4a, 4b, 4c, and `plugins/orb/skills/review-spec/SKILL.md` Pass 1 rule 5

No unexpected files. No secrets. No unrelated churn.

## Test Results

The orbit plugin has no executable test harness (no pytest, no bats, no cargo test; skills are markdown-interpreted by Claude). The verification strategy, confirmed by the caller's brief and by the implementer's `progress.md` §Notes, is:

- **Markdown-skill changes** for behavioral ACs that Claude executes (ac-02, ac-04, ac-05, ac-06, ac-07, ac-10, ac-11, ac-12) — verified by reading the skill documents and confirming the exact contract clauses are present and unambiguous.
- **Executable smoke tests** for the shell/file layer (ac-01, ac-03, ac-08, ac-09) — I re-ran these independently against `/tmp/mrl-review` to confirm behaviour (output captured below).

```
| Check                                     | Result | Details |
|-------------------------------------------|--------|---------|
| Executable tests (N/A — no harness)       | EXEMPT | documented in spec; accepted strategy |
| Smoke: matching-hash no drift             | PASS   | session-context.sh output silent on drift |
| Smoke: stale-hash emits drift notice      | PASS   | literal "orbit: spec modified..." line matches DRIFT_NOTICE |
| Smoke: missing-hash silent skip           | PASS   | no drift notice, exit 0 |
| Smoke: gate at ac-01 blocks               | PASS   | "ac-01 is a blocking gate. ac-02 becomes startable..." |
| Smoke: all-checked terminal               | PASS   | "No unchecked ACs remain..." |
| Smoke: non-gate next AC                   | PASS   | "Next AC — ac-02" |
| Smoke: parser ignores ## Detours content  | PASS   | `[x] ac-02` inside Detours did not mark ac-02 done |
| AC coverage (mrl_ac01..12)                | 12/12 — 4 via direct re-run, 8 via skill-doc verification |
```

## AC Coverage Report (prefix: mrl)

I assessed each AC against the actual diff rather than trusting `progress.md`. For code-typed ACs that manifest as shell behaviour I re-ran the hook; for code-typed ACs that manifest as implement-skill runtime behaviour I verified the contract clauses are documented explicitly enough for the LLM-interpreted skill to comply.

```
| AC    | Type | Status  | Evidence                                                                     |
|-------|------|---------|------------------------------------------------------------------------------|
| ac-01 | code | PASS    | progress.md line 2: `Spec hash: sha256:2899385272...d8b4b` matches `sha256sum spec.yaml` exactly (289938527266aa32a0ebb31b4741a741b91370a764b0ad609341f2fae54d8b4b). Template in implement/SKILL.md §4 specifies the exact field format. |
| ac-02 | code | PASS*   | implement/SKILL.md §4b(2) pins the interactive/non-interactive branch, the two AskUserQuestion option strings verbatim, the acknowledgement contract, and the non-interactive exit-1 behaviour. No executable test. The two AskUserQuestion option strings in §4b(2) lines 125–126 match the spec's verification fixture (ii) exactly. |
| ac-03 | code | PASS    | session-context.sh lines 215–229: field-absent path (recorded_hash empty → no drift), mismatch path (stderr-equivalent echo of $DRIFT_NOTICE). Re-ran fixture (a) mismatch → notice printed; fixture (b) absent → no notice, exit 0; fixture (c) matching → no notice. |
| ac-04 | code | PASS*   | implement/SKILL.md §4 template has `Current AC: <ac-id | none>` (line 82); §4c(2) specifies "Immediately set `Current AC:` to the entry's `Return to:` target". No standalone test; documented contract is explicit enough for the interpreting skill. |
| ac-05 | code | PASS*   | implement/SKILL.md §4 template places `## Detours` between `## Hard Constraints` and `## Acceptance Criteria` (lines 84–89); §4c(1) specifies atomic append format `YYYY-MM-DD: <desc>` + `Return to: <ac-id>`; line 105 states "Never edit or remove an entry after writing." Project's own progress.md demonstrates the section placement (lines 26–28). |
| ac-06 | code | PASS*   | implement/SKILL.md §4b(3) line 128: "Walk the AC list in declaration order... If any preceding AC with `ac_type: gate` is not marked `[x]`, refuse to start the current AC and emit a refusal message naming the blocking gate's id. Non-gate preceding ACs do **not** block". Matches all three fixture cases (a/b/c) in spec verification. |
| ac-07 | code | PASS*   | implement/SKILL.md §4c spells out the three-step re-anchor sequence verbatim matching the spec verification's (i)/(ii)/(iii) asserts — set Current AC, re-read progress.md + AC list, select from first unchecked AC in ## Acceptance Criteria. |
| ac-08 | code | PASS    | session-context.sh lines 235–279 implement next-AC surfacing and gate-aware naming. Re-ran fixture (a) non-gate next → "Next AC — ac-02"; fixture (b) gate next with follow-on AC → "ac-01 is a blocking gate. ac-02 becomes startable once ac-01 closes."; fixture (c) all checked → "No unchecked ACs remain...". All three assertions hold. |
| ac-09 | code | PASS    | session-context.sh lines 235–247 awk parser: `in_ac` flag set only inside `## Acceptance Criteria`; any `## `-prefixed heading resets it. Verified with a `- [x] ac-02` line planted inside `## Detours` alongside `- [ ] ac-02` in `## Acceptance Criteria` — the hook correctly surfaced ac-01 (first unchecked) without counting the Detours line. |
| ac-10 | doc  | PASS    | grep confirms `Spec hash:`, `Current AC:`, `## Detours`, `- [ ] ac-NN (gate):` all present in implement/SKILL.md §4 fence (lines 80/82/87/90). DRIFT_NOTICE declared once in implement/SKILL.md line 114 and referenced literally once in session-context.sh line 207 — single-source contract intact. |
| ac-11 | code | PASS*   | review-spec/SKILL.md Pass 1 rule 5 (lines 45–50) adds deterministic gate-verification check with the exact three rules (non-empty, placeholder-token set case-insensitive, ≥20 chars) and the exact placeholder token list `{TBD, TODO, FIXME, PLACEHOLDER, XXX, ???}` matching the spec fixture expectations. The rule is embedded in Pass 1, runs before the content-signal scan. No LLM-judgement step. |
| ac-12 | code | PASS*   | implement/SKILL.md §4b(1) specifies the backfill: compute hash, insert line, emit log `Backfilled Spec hash for existing progress.md`, NOT emit drift notice; §4b's three-step pre-AC ordering (backfill → drift → gate) is stated explicitly on line 120, so ordering fixture (d) holds by construction. session-context.sh AC-03 path covers 12(c) — confirmed with the missing-hash fixture. |

Coverage: 12/12 testable ACs satisfied (11 code + 1 doc); 4 with direct re-run evidence, 8 with skill-doc evidence (marked PASS*).
```

### Caveat on PASS* rows

For ACs whose implementation is a contract the LLM-interpreted `implement` skill must honour, I cannot run a unit test — the runtime IS the LLM. The contract clauses are explicit, unambiguous, and cite the exact strings / sequence / field semantics the spec verifications require. The spec itself establishes this strategy ("Verification deliverables not shipped as executable tests in this iteration — the ACs' `mrl_ac*` fixture assertions describe the intended test shape; they land as executable suites when the orbit plugin grows a test harness"). I accept it for this review.

## Phase 3: Environment Simulation

`session-context.sh` is invoked by Claude Code at session start. I simulated two realistic deployment contexts:

1. **Matching-hash session (steady state):** no drift notice, just the next-AC line — confirmed clean output, no false-positive noise.
2. **Stale-hash session (after an author edits spec.yaml):** drift notice prints BEFORE the next-AC line, both appearing before the existing "Next: implement" nudge. Ordering is correct — the author sees the drift warning first.

I did not test the MacOS `shasum -a 256` fallback branch (line 223) directly, but the code path is trivially equivalent and both tools read files in binary mode by default.

## Phase 4: Edge Case Probing

```
| Probe                                               | Result | Note |
|-----------------------------------------------------|--------|------|
| First run — no progress.md                          | PASS   | `has_progress=0` gates the entire block; hook skips silently |
| Empty ## Acceptance Criteria section                | PASS   | awk loop produces no match → "No unchecked ACs remain" branch fires (line 277) |
| ## Detours contains legitimate-looking `[ ] ac-XX`  | PASS   | awk `in_ac` discipline prevents contamination — verified |
| Hash field present but malformed (e.g. truncated)   | OK     | `current_hash != recorded_hash` fires → drift notice (slightly false-positive; acceptable per spec's raw-bytes trade-off) |
| macOS/Linux sha tool branch                         | PASS   | `command -v sha256sum` then `shasum -a 256` fallback — both read binary by default |
| Idempotent re-run on matching state                 | PASS   | hook is stateless; no side effects on progress.md — safe to run N times |
| Non-interactive drift path (ORBIT_NONINTERACTIVE=1) | PASS*  | Documented in implement/SKILL.md §4b(2); no executable harness to exercise it, but the contract is pinned to exit status 1 and "do not call AskUserQuestion" |
```

No edge-case failures.

## Decision Register Keyword Scan

Ran a keyword scan across `orbit/decisions/` using terms `progress.md`, `drift`, `spec hash`, `session-context`, `gate`. No prior decision constrains how progress.md is structured, how drift is detected, or how gates are enforced. Decisions 0004 (drive verdict contract) and 0009 (rally parallel drive-full) mention "drift" only in the generic software-engineering sense (divergence between duplicated logic), not in this spec's sha256-hash sense. **No conflicts with existing decisions.**

## Constraint Compliance

Spot-checked all 16 constraints in spec.yaml against the diff:

- **Card 0009 owns schema** — implement/SKILL.md §4 ("the schema is owned by card 0009") ✓
- **Three-layer non-negotiable** — state/surface/behaviour all present ✓
- **Reuse ac_type: gate, no new field** — no gate:/blocks: added anywhere ✓
- **Detours inside progress.md, no findings.md** — line 105 explicitly forbids findings.md ✓
- **Detours atomic/terminal** — §4c(1) "the append IS the resolution"; no Status field anywhere ✓
- **sha256-based, no Monitor-tool** — hash only; no watchdog ✓
- **Raw bytes, no normalisation** — §4a line 98 "no line-ending conversion, no trimming, no YAML canonicalisation" ✓
- **Canonical DRIFT_NOTICE single source** — implement/SKILL.md line 114 + session-context.sh line 207, identical string; grep returned exactly 2 occurrences ✓
- **rally.yaml untouched** — `git diff --stat` confirms no rally.yaml change ✓
- **review-spec Pass 1 secondary only** — rule 5 documented; runtime enforcement stays in implement/SKILL.md §4b(3) ✓
- **Parser ignores ## Detours** — session-context.sh awk `in_ac` discipline + empirical verification ✓
- **Authoritative template verbatim** — interview Q5 template present in §4 fence ✓
- **Non-gate ACs don't block** — §4b(3) line 128 explicit ✓
- **Backwards compat** — §4b(1) + session-context.sh silent-skip ✓
- **Pre-AC sequence (backfill → drift → gate)** — §4b states this ordering on line 120 and implements it in §§4b(1)–(3) ✓
- **ac-02 non-interactive path** — §4b(2) "Non-interactive path" bullet ✓

All 16 constraints honoured.

## Findings

### [LOW] `has_progress` variable is computed but dormant in the non-mission-resilience branch
**Category:** code-quality (non-blocking)
**Description:** `session-context.sh` line 200 computes `has_progress` unconditionally, but the only consumer is the new block at line 209. This is harmless — it simply becomes a one-line cost on every session start — but it could have been scoped inside the new block.
**Evidence:** `session-context.sh:200` and `session-context.sh:209`.
**Recommendation:** Optional cleanup on a future pass; not merge-blocking.

### [LOW] Drift notice is emitted to stdout in session-context.sh but the spec sometimes says "stderr"
**Category:** spec-fidelity (cosmetic)
**Description:** spec.yaml ac-03 says "stdout contains the canonical drift-notice string" (so the hook is correct), and ac-02 says "stderr/stdout" (flexible). The hook uses stdout. Consistent with the spec as written — flagging only because the mixed wording in ac-02 and ac-03 may confuse future readers. No change needed.
**Evidence:** spec.yaml lines 29 (ac-02 "stderr/stdout") vs 34 (ac-03 "stdout"); session-context.sh line 228 uses stdout.
**Recommendation:** Accept as-is.

### [LOW] Two deferred AC behaviours cannot be verified without a test harness
**Category:** test-gap (accepted)
**Description:** ac-02 non-interactive path, ac-07 re-anchor correctness, ac-12 backfill ordering, and the ac-11 review-spec rule application all depend on the LLM-interpreted skills obeying the documented contracts. No executable test exists. This is accepted per the spec's explicit "test harness is future work" stance, but the next iteration that adds a test runner should promote these to passing executable checks (bats or pytest against the skills — or a dedicated `mrl_ac*` suite as named).
**Evidence:** No `mrl_ac*` files found outside spec/review documents. Grep over the repository returns 5 files, all `spec.yaml` / review markdown.
**Recommendation:** Open a follow-up card when a test harness lands; not merge-blocking for this spec.

No CRITICAL, HIGH, or MEDIUM findings.

## Honest Assessment

The three-layer mechanism is cleanly implemented: `progress.md` template is the durable state authority, `session-context.sh` is the resume-time notifier, and `implement/SKILL.md` §§4a–4c is the in-session behaviour contract. The executable shell layer (drift check, next-AC surfacing, parser discipline) is correct against all 8 fixture assertions I re-ran independently. The LLM-interpreted skill layer is documented with the exact contract strings, field formats, and sequencing the spec requires — including the ac-02 non-interactive branch with explicit exit statuses and the fixed pre-AC ordering (backfill → drift → gate). The single-source DRIFT_NOTICE contract is intact (grep confirms exactly one declaration and one literal-include). Constraint compliance is 16/16. The card's `specs:` field was updated to reference the spec as required by the repo conventions. The absence of executable tests is a real gap, but an honestly-declared one — the spec itself pins it as deferred, and the project-level progress.md §Notes is transparent about the strategy. The diff does not claim more than it delivers, and the four ACs I could re-run all passed. I recommend merging.
