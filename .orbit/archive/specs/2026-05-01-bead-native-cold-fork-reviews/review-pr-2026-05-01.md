# Pre-Merge Review

**Date:** 2026-05-01
**Reviewer:** Context-separated agent (fresh session)
**Branch:** drive/0016-bead-native-cold-fork-reviews
**Spec:** .orbit/specs/2026-05-01-bead-native-cold-fork-reviews/spec.yaml
**Verdict:** APPROVE

---

## Test Results

```
| Check                          | Result | Details                                                |
|--------------------------------|--------|--------------------------------------------------------|
| test-gate-ac-verification.sh   | PASS   | ac-01 PASS / ac-02 FAIL on placeholder+length (exit 0) |
| test-promote-gate-propagation  | PASS   | gate scenario emits [gate]; non-gate clean (exit 0)    |
| AC coverage (substantive)      | 13/13  | See AC report below                                    |
| AC coverage (strict-rule)      | 11/13  | Two byte-level verification fragilities; see findings  |
```

This repo has no general project test suite — tests for this spec are
the two bash fixtures committed under `plugins/orb/scripts/tests/`.
Both pass.

## AC Coverage Report

```
| AC    | Status | Substrate evidence                                                                                                  |
|-------|--------|---------------------------------------------------------------------------------------------------------------------|
| ac-01 | OK     | All six greps return zero matches; §1.x / §3.x cross-refs consistent post-renumber                                  |
| ac-02 | OK     | Stage 1 brief example carries <bead-id>, output path, bd show, parse-acceptance.sh; verdict line preserved          |
| ac-03 | OK     | Stage 3 brief example carries <bead-id>, diff ref, bd show, parse-acceptance.sh; verdict line preserved             |
| ac-04 | OK     | review-spec §1 renamed "Gather the Bead"; spec.yaml/interview_ref absent; bd show + parse-acceptance.sh present     |
| ac-05 | OK     | Three rules preserved byte-for-byte; substrate note names is_gate + description; ac_type: gate phrase removed       |
| ac-06 | PARTIAL| Substrate operation correct; one byte-level verification violation (see Findings)                                   |
| ac-07 | OK     | Completion section drops "bead snapshots" + "snapshot path"; commit/PR title formats preserved                      |
| ac-08 | OK     | test-gate-ac-verification.sh exits 0; ac-01 PASS, ac-02 FAIL on placeholder+length                                  |
| ac-09 | OK     | MADR 0013 exists, status accepted, all five points (a–e) addressed; decision 0002 superseded                        |
| ac-10 | OK     | Card 0016 specs[] contains the spec path; maturity flipped to active                                                |
| ac-11 | OK     | Card SKILL.md doc; promote.sh emits [gate]; card 0016 scenario 2 annotated; dry-run shows [gate] on ac-02; test PASS|
| ac-12 | OK     | Both inline defaults moved to orbit/reviews/<bead-id>/...; old YYYY-MM-DD-<topic> path absent                       |
| ac-13 | PARTIAL| File rename complete; CHANGELOG updated; only stale refs remain inside this spec's own artefacts (see Findings)     |
```

## Findings

### [LOW] review-pr §2 still names `progress.md` in explanatory prose
**Category:** test-gap (verification contract)
**Description:** ac-06 verification rule (b) requires the literal string `progress.md` to be absent from §2 and §3 of `plugins/orb/skills/review-pr/SKILL.md`. One reference remains:
**Evidence:** `plugins/orb/skills/review-pr/SKILL.md:33` — "The bead acceptance field replaces the earlier `progress.md` tracker — `[x]` marks are the implementer's self-reported AC completions, set by `/orb:implement` via `parse-acceptance.sh check`."
**Recommendation:** This is contextual prose orienting reviewers about what `[x]` marks mean — substantively the skill operates on bead substrate, not on progress.md. Two viable fixes if strict adherence to the verification rule matters: (a) drop the `progress.md` clause and rephrase as "`[x]` marks are the implementer's self-reported AC completions, set by `/orb:implement` via `parse-acceptance.sh check`"; or (b) accept the prose as a deliberate transitional aid and note in spec metadata that ac-06 (b) is satisfied substantively. The substrate wiring is correct either way; this is purely a byte-level rule fragility against a reviewer-helpful transitional sentence.

### [LOW] ac-13 stale-reference grep returns 8 matches inside spec folder
**Category:** test-gap (verification contract)
**Description:** ac-13 verification rule (c) requires `git grep -F '0011-design-intent-not-means' .` to return zero matches. It returns 8.
**Evidence:** All 8 matches are inside `.orbit/specs/2026-05-01-bead-native-cold-fork-reviews/` — the spec.yaml's own constraint/AC text (which describes the rename), `progress.md` (which logs the rename), and the two prior cycle review files (which discussed the collision before it was resolved). No matches in `.orbit/choices/`, `plugins/orb/skills/`, `.orbit/cards/`, `CHANGELOG.md`, or any other live artefact. The CHANGELOG cross-ref is correctly updated to `0012-design-intent-not-means`.
**Recommendation:** The intent of ac-13 — eliminate stale cross-references in active artefacts — is fully met. The verification rule's `.` scope was unintentionally over-broad: it captures the very spec/progress/review files that necessarily reference the rename to describe it. Treat as satisfied substantively. If a future spec wants to enforce this kind of cleanup, scope the grep to the live-artefact directories (`.orbit/choices/`, `plugins/orb/skills/`, `.orbit/cards/`, `CHANGELOG.md`) and exempt the `.orbit/specs/<the-spec-doing-the-rename>/` folder.

---

## Honest Assessment

This is a clean, comprehensive cutover. Every substantive piece of the spec lands: the snapshot bridge is fully removed from drive, both review skills read beads directly, gate semantics propagate end-to-end (card scenario `gate: true` → `promote.sh` → `[gate]` marker → parser `is_gate=1` → review-spec Pass-1 deterministic check), MADR 0013 documents the substrate decision with full S1–S5 analysis and a substrate-mapping table, decision 0002 is correctly partially superseded, and the verdict-line + verdict-path contracts are preserved byte-for-byte. The two test fixtures are well-constructed, self-contained, and exit 0. The drive cross-reference renumber is internally consistent — every `§1.x` / `§3.x` reference points to a section that exists post-deletion.

The two PARTIAL ratings are both verification-rule fragilities, not substrate or contract violations. The `progress.md` reference in review-pr §2 is reviewer-helpful transitional prose that names the artefact being replaced; the live skill operates on the bead. The `0011-design-intent-not-means` matches are all inside this spec's own folder, describing the rename — no live cross-reference is stale. Both could be cleaned up in a small follow-up edit if strict compliance with the byte-level verification commands matters; neither blocks merge or compromises the substrate cutover.

Substantive risks I looked for and did NOT find: dual code paths (none — hard cutover holds); auto-detect logic (none); residual snapshot writes (none in drive SKILL.md or its Worked example); drift between implement and review parsers (none — all three call `parse-acceptance.sh acs <bead-id>`); verdict-regex modification (regex preserved verbatim at line in drive SKILL.md); broken resumption table cross-ref (correctly renumbered to §1.2).

Recommend APPROVE.
