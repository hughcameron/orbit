**Verdict:** APPROVE

**Date:** 2026-05-24
**Reviewer:** Context-separated agent (fresh session, Opus 4.7)
**Branch:** rally/setup-is-orbit-state-aware (1 commit ahead of origin)
**Spec:** 2026-05-24-setup-is-orbit-state-aware

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite (cargo test --workspace) | PASS | 497 passed, 0 failed (8 suites, 2.34s) |
| Classifier targeted tests | PASS | 8/8 (`cargo test --workspace classifier`) |
| substrate.classify parity tests | PASS | 8/8 (CLI + MCP across idempotent / wrapped-undotted / brownfield-bare shapes) |
| topology.setup branch tests | PASS | 16/16 (plugin-repo + README-only branches) |
| decisions-md unmigrated tests | PASS | 3/3 (fires / suppressed / empty-dir) |
| ac-20 suppression test | PASS | 1/1 (canonical-files-missing absent on wrapped-undotted) |
| Live `orbit substrate classify` on this repo | PASS | returns `idempotent`; envelope `{"state":"idempotent"}` |
| Live `orbit audit conformance` on this repo | PASS | no `canonical-files-missing` findings (only memo-staleness, unrelated) |
| Live `plugin_repo: true` grep on `.orbit/config.yaml` | PASS | line present, exit 0 |
| AC coverage | 21/21 | every AC has matching evidence (see below) |

## AC Coverage Report

Walk each AC against the typed-evidence table from this skill's §3. ACs 01–10 are skill-prose driven (the SKILL.md surface IS the contract — there's no shipping binary for the agent prompt itself); ACs 11–21 are mostly `code` and carry tests.

| AC | ac_type | Status | Evidence |
|----|---------|--------|----------|
| ac-01 | code | ✓ skill-prose | SKILL.md §0a (lines 24-32) — orbit-state binary missing → exit non-zero, no mutation |
| ac-02 | code | ✓ skill-prose | SKILL.md §0b (lines 34-42) — orbit-state < 0.4.33 → refuse with version message |
| ac-03 | code | ✓ skill-prose | SKILL.md §2 (lines 63-77) — greenfield creates `.orbit/{cards,specs,choices,memos}` then §6 |
| ac-04 | code | ✓ skill-prose | SKILL.md §6b (lines 276-288) — byte-for-byte compare, prompt on drift, no silent overwrite |
| ac-05 | code | ✓ skill-prose | SKILL.md §6a (lines 253-272) — legacy marker detection, atomic migrate-or-refuse |
| ac-06 | code | ✓ skill-prose | SKILL.md §3.W (lines 168-198) — wrapped-undotted prompt + `git mv orbit .orbit` |
| ac-07 | code | ✓ skill-prose + code | SKILL.md §5 (lines 228-237) — no-op + `plugin_repo` preservation invariant |
| ac-08 | code | ✓ | plugins/orb/skills/drive/SKILL.md §Input contract (lines 32-48) — `BLOCKED: drive requires an initialised .orbit/ substrate` |
| ac-09 | code | ✓ skill-prose | SKILL.md §7 (line 366) — "Setup does NOT auto-run a drive" |
| ac-10 | code | ✓ | SKILL.md §1 table (lines 50-57) — exactly 6 rows; conditions non-overlapping, exhaustive |
| ac-11 | code | ✓ | `classifier_returns_*` (8 tests in verbs.rs:12114+); `pub fn classify_substrate_layout` (verbs.rs:4570); used by `audit_conformance_at` (verbs.rs:3987) — single source of truth |
| ac-12 | code | ✓ | `topology_setup_writes_readme_when_plugin_repo_unset`, `topology_setup_writes_seeds_when_plugin_repo_true_and_paths_exist`, `topology_setup_idempotent_readme_branch` (verbs.rs); `Config::plugin_repo` field (schema.rs:641) |
| ac-13 | code | ✓ | `topology_setup_rejects_when_plugin_repo_true_and_canonical_code_missing` (verbs.rs); live probe returns `{"error":{"category":"not-found","message":"seed `cards` canonical_code path does not exist..."}}` |
| ac-14 | code | ✓ skill-prose | SKILL.md §3c (line 93) `decisions/   → .orbit/decisions/  (MADR files; manual MD→YAML conversion needed)`; §3d (line 114) `git mv decisions .orbit/decisions`; §3f.1 warning paragraph |
| ac-15 | code | ✓ | `decisions_md_finding_fires_for_unconverted_file`, `_suppressed_when_choice_yaml_exists`, `_empty_when_decisions_dir_absent` (verbs.rs); live probe surfaces `decisions_md_unmigrated .orbit/decisions/0001-foo.md` |
| ac-16 | doc | ✓ | SKILL.md:3 reads `creates .orbit/ directory` — grep confirms |
| ac-17 | config | ✓ | `grep -F 'plugin_repo: true' .orbit/config.yaml` exit 0; diff shows the line added |
| ac-18 | code | ✓ | `substrate_classify_cli_{idempotent,wrapped_undotted,brownfield_bare}_envelope` × 2 surfaces (CLI tests/parity.rs:1618+, MCP tests/parity.rs:787+); shared `expected_envelope_for_substrate_classify` helper enforces byte-equality |
| ac-19 | code | ✓ | `classifier_returns_mixed_bare_when_dotted_and_bare_coexist` + `mixed_undotted_when_dotted_and_orbit_coexist`; SKILL.md §4 refusal prose with no-mutation invariant snippet (lines 200-226) |
| ac-20 | code | ✓ | `audit_conformance_suppresses_canonical_files_missing_on_wrapped_undotted` (verbs.rs:12351+); live probe on wrapped-undotted fixture returns empty `findings` (suppressed) |
| ac-21 | doc | ✓ | SKILL.md §3 (lines 79-118) preserved structurally: 3a-3f prompt/git-mv/residue/dirty-tree flow intact; only state slug rename + decisions/ target updated per ac-14 |

Coverage: **21/21 ACs have evidence (100%).** All code-typed ACs that needed unit tests have them; the skill-prose ACs (ac-01..ac-10 first batch) close on the SKILL.md surface itself (the prompt IS the implementation for agent-driven steps).

## Findings

### [LOW] Spec carries default `ac_type` (`code`) on skill-prose ACs that have no executable surface
**Category:** test-gap (typing, not missing tests)
**Description:** ac-01 through ac-09 are agent-driven prompts/refusals delivered through SKILL.md prose — they have no Rust function to unit-test. The spec leaves them at the default `ac_type: code`, which technically obligates "passing test bearing the AC id + commit reference" per the §3 evidence table. The implementer covered them through skill-prose, which is the correct shape, but the AC typing doesn't reflect that. Worth tagging these as `ac_type: doc` (the evidence shape that actually closes them) on the next pass so future reviews don't have to derive the routing manually.
**Evidence:** spec.yaml AC entries ac-01..ac-09 lack `ac_type` field (defaults to `code`); compare ac-16 / ac-17 / ac-21 which are typed correctly.
**Recommendation:** In a follow-up pass — not as a blocker for this PR — add `ac_type: doc` to ac-01, ac-02, ac-04, ac-05, ac-09 (pure skill-prose); leave ac-03, ac-06, ac-07, ac-08 as `code` since they each have backing tests or live verification on top of the prose surface. Capture as a memo if it falls outside this drive's scope.

### [INFO] `mixed-bare` / `mixed-undotted` refusal mechanics live in SKILL.md prose, not in a binary
**Category:** observation
**Description:** ac-19 says "Verifiable by integration test fixtures for each shape" and the classifier-side has unit tests that confirm the right `MixedBare` / `MixedUndotted` enum is returned. The actual refusal-with-no-mutation invariant lives in SKILL.md §4 prose (lines 200-226) and is enforced at agent execution time, not by a verb. The implementer's choice is consistent with the rest of the setup skill (which is prose-driven), and the unit-test coverage for the classifier closes the load-bearing half — the agent reads the state and refuses. Not blocking.
**Evidence:** verbs.rs classifier tests cover both states; SKILL.md §4 prose has the explicit no-mutation invariant snippet. There is no end-to-end integration test that simulates the agent's refusal path.
**Recommendation:** None. This pattern is already established across the setup skill — calling it out for visibility, not change.

---

## Honest Assessment

This is a clean, well-scoped implementation. Every AC has matching evidence, the test suite is green (497/497), and live verification on this repo confirms the four most consequential behaviours: substrate.classify returns the right state, audit conformance suppresses canonical-files-missing on wrapped-undotted, topology.setup branches correctly on `plugin_repo`, and the canonical_code path validation refuses missing paths with a clean error. The classifier helper as shared substrate (ac-11) is the architectural move worth flagging — single source of truth between SKILL.md prose and audit logic, exactly the kind of substrate accretion the orbit method calls for. The two findings above are typing/observation hygiene, not gaps.

APPROVE.
