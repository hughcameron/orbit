# Pre-Merge Review

**Date:** 2026-05-21
**Reviewer:** Context-separated agent (fresh session)
**Branch:** main (commits 1c0c9f2..bb717fa already merged; review-against-baseline-1929506)
**Spec:** 2026-05-21-tabletop
**Verdict:** REQUEST_CHANGES

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite (`cargo test` in `orbit-state/`) | PASS | 446 passed, 8 suites, 2.17s |
| `orbit audit conformance --json` | CLEAN | No drift findings; surfaces two cards `ready_for_tabletop` (expected substrate signal, not a cascade gap) |
| AC coverage (type-keyed) | 11/12 satisfied | ac-11 has live-surface residue — see findings |

## AC Coverage Report

All 12 ACs are `ac_type: doc` (11) or `ac_type: observation` (1). No `code`-type ACs — coverage is verified by grep + file inspection against the SKILL.md, METHOD.md×3, the conformance engine, the new MADR choice files, and the probe artefact. The orbit-state test suite green is corroborating evidence for ac-11(4).

| AC | Type | Status | Verification |
|----|------|--------|--------------|
| ac-01 | doc | OK | `plugins/orb/skills/tabletop/SKILL.md` lines 113-198 codify Q1-Q10 in declared order with role + stop conditions; line 115 references the 2026-05-07 dogfood as worked example. |
| ac-02 | doc | OK | SKILL.md §Output (lines 280-321) prescribes `tabletop.md` sidecar with six sections in declared order. Mutual exclusion with `tabletop-note.md` named in line 107 and 324. Choice 0027 (`tabletop-contract-sidecar`) ships the rationale. No `Spec` schema change — verified via `orbit-state/crates/core/src/schema.rs` carries no new fields. |
| ac-03 | doc | OK | SKILL.md §One-to-many fan-out (lines 326-340) names the N ≥ 1 contract, per-spec folder layout, sibling-sidecar header convention. |
| ac-04 | doc | OK | SKILL.md §Mode B (lines 45-58) covers goal-string input with AUQ cluster confirmation. Inference is best-effort (substring / semantic / keyword scan named); AUQ is the safety valve. |
| ac-05 | doc | OK | SKILL.md §Trivial-skip advisory (lines 31-37) is prose, not AUQ, and does not refuse. |
| ac-06 | doc | OK | SKILL.md §Halt / escalation / kill — actionable shape (lines 242-278) names rejected anti-patterns and canonical shapes for halts / escalations / kill conditions. |
| ac-07 | doc | OK | SKILL.md §Real scenarios over imagined (lines 222-240) directs Q4/Q5 to past run-logs first; `[imagined]` marker convention covered. |
| ac-08 | doc | OK | SKILL.md §AUQ-prose hybrid (lines 199-220) tabulates per-question mode; inflation-guard recut named in ac-08 description and line 183 (Q9). AUQ-refusal fallback covered lines 218-220. |
| ac-09 | doc | OK | SKILL.md §Design-space classification (lines 60-72) and §Closed mode (lines 74-111) port `/orb:design`'s §3-§4 and the tabletop-note shape. Ordering ("classification runs AFTER cluster confirmation") at line 58. |
| ac-10 | observation | OK | `.orbit/specs/2026-05-21-tabletop/ambiguity-floor-probe.md` carries baseline (0.165, 2026-05-19-act-when-authorised), tabletop (0.200, card 0013), per-axis breakdowns, GO verdict. The criterion was amended in the AC text (see findings F2 for note on this). NO-GO branch artefact `.orbit/memos/2026-05-21-tabletop-nogo.md` exists and is explicit about the amendment. |
| ac-11 | doc | **PARTIAL** | Six cascade steps mostly landed cleanly, but step (7)'s self-check grep was not honestly re-run — see F1. |
| ac-12 | doc | OK | Choice 0026 (`tabletop-replaces-design`, status `accepted`) and choice 0027 (`tabletop-contract-sidecar`, status `accepted`) exist, both cite choice 0017, both cite spec 2026-05-21-tabletop and card 0019, both name the rejected alternatives. |

## Findings

### [MEDIUM] F1 — ac-11 cascade left three live-surface `ready_for_design` references

**Category:** test-gap (cascade self-check missed) / environment-mismatch (live agent prose still names the retired slug)

**Description:** ac-11 step (7) explicitly contracted the implementing agent to re-run `rg "(/orb:)?design|design-note|ready_for_design" plugins/orb/skills/*/SKILL.md CLAUDE.md README.md orbit-state/crates/core/src/verbs.rs .orbit/METHOD.md` and confirm only intentional references remained. That re-run was not honestly performed — three live-surface references survive:

1. `plugins/orb/skills/setup/SKILL.md:247` — narrative `"ready for design"` (the parenthetical describing the card-state finding's trigger condition).
2. `plugins/orb/skills/setup/SKILL.md:251` — explicit slug `ready_for_design` in the parked-card carve-out paragraph (`excluded from the \`ready_for_design\` finding silently`).
3. `plugins/orb/skills/card/SKILL.md:132` — explicit slug `ready_for_design` in the parked-card section (`no \`ready_for_design\` finding, no envelope trace`).

The conformance engine in `verbs.rs` no longer emits `ready_for_design` — it emits `ready_for_tabletop` (verified at `verbs.rs:1138`, `:3833`, `:9957`, and live envelope: `orbit audit conformance --json` returns `"state":"ready_for_tabletop"` for cards 0013 and 0014). Live prose in two consumer SKILL.md files therefore describes a slug that no longer exists in the substrate. ac-11(4b) explicitly added `plugins/orb/skills/prioritise/SKILL.md` to the slug-sweep set as the canonical downstream-consumer pattern; the setup and card SKILL.md files are the same downstream-consumer pattern and were missed.

**Evidence:**
```
$ rg -n "ready_for_design" plugins/orb/skills/ orbit-state/crates/core/src/verbs.rs
plugins/orb/skills/card/SKILL.md:132:...silently — no `ready_for_design` finding, no envelope trace...
plugins/orb/skills/setup/SKILL.md:251:...excluded from the `ready_for_design` finding silently...
(no matches in verbs.rs — the slug was renamed)

$ orbit audit conformance --json | jq '.data.result.findings[0].state'
"ready_for_tabletop"
```

**Recommendation:** A small follow-up commit on the cascade:

- `plugins/orb/skills/setup/SKILL.md:247` — `(ready for design)` → `(ready for tabletop)`
- `plugins/orb/skills/setup/SKILL.md:251` — ``the `ready_for_design` finding`` → ``the `ready_for_tabletop` finding``
- `plugins/orb/skills/card/SKILL.md:132` — ``no `ready_for_design` finding`` → ``no `ready_for_tabletop` finding``

Then re-run the canonical detection grep from ac-11(7) and confirm only intentional `/orb:design` historical references remain. The four `/orb:design` references in `plugins/orb/skills/tabletop/SKILL.md` (lines 10, 27, 29, 344) are all intentional — they name the retired predecessor where the historical reference is load-bearing (failure mode being replaced, replacement notice, ported section attribution, legacy artefact compatibility note).

### [LOW] F2 — ac-10 criterion was amended mid-flight; the amendment trail is honest but worth flagging

**Category:** governance / process clarity

**Description:** ac-10's text says it was *"Amended 2026-05-21 from the original strict parity criterion based on probe evidence"* — the original criterion was strict `tabletop ≤ baseline = GO`, and the amendment added an `OR ≤ 0.2 (project bar)` clause. The probe artefact (`ambiguity-floor-probe.md`) records the amended verdict (GO). The NO-GO memo (`.orbit/memos/2026-05-21-tabletop-nogo.md`) is explicit that the *probing agent* "invented an 'OR project bar' clause not in the AC text" — and frames a fourth option (d) which is the amendment that was eventually adopted. So the amendment trail is fully captured on disk and the substrate is honest about what happened. This is not a blocking concern; it is a process observation worth marking because the precedent ("amend the AC after the probe, based on probe-derived evidence about rubric noise") is one the reviewer should not let drift into a reflexive habit. Future ambiguity probes should aim to set the criterion correctly the first time.

**Evidence:** Compare ac-10 final text in `.orbit/specs/2026-05-21-tabletop/spec.yaml:54` (carries the amendment) against `.orbit/memos/2026-05-21-tabletop-nogo.md:1-44` (the gap analysis that motivated the amendment and named option (d)).

**Recommendation:** No code change. Worth a substrate memory (`orbit memory remember`) capturing the pattern — *"when an ambiguity-floor probe lands borderline, the gap analysis itself can motivate an AC-text amendment within the same session; do not let this become reflexive — set the criterion correctly the first time"*. The implementing agent's honesty in the NO-GO memo is the right behaviour; only the *prospect* of this becoming routine is the concern.

---

## Honest Assessment

The tabletop skill ships cleanly. The SKILL.md is comprehensive, well-structured, and the 10-question methodology with AUQ-prose hybrid is faithfully drawn from the 2026-05-07 dogfood. The two MADR choice files (0026, 0027) record the right rationale and cite the right antecedents. The conformance engine cascade — five sub-steps in `verbs.rs` — landed completely and the 446-test suite is green, which is the load-bearing evidence ac-11(4) actually shipped. The amendment to ac-10 was governed in the open (the NO-GO memo names what the probing agent did and why the criterion was relaxed) and the final GO call rests on a substantively justified gap analysis (the methodology delivered its structural win on `constraint_clarity` and lost on `criteria_clarity` for domain-driven reasons rather than methodology weakness).

The single blocker is F1: the ac-11(7) self-check was not honestly re-run, and three live-surface references to the retired `ready_for_design` slug remain in `plugins/orb/skills/setup/SKILL.md` (×2) and `plugins/orb/skills/card/SKILL.md` (×1). The substrate now describes a slug that the conformance engine doesn't emit, which is the exact drift the cascade was designed to eliminate. The fix is three line-level edits and a re-run of the detection grep — small in cost, but worth doing before declaring the cascade complete.

F2 is a flag, not a block. It records the precedent so the pattern doesn't normalise.
