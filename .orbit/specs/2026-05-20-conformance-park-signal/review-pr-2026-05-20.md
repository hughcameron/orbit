# Pre-Merge Review

**Date:** 2026-05-20
**Reviewer:** Context-separated agent (fresh session)
**Branch:** main (implementation in commit c1345d6)
**Spec:** 2026-05-20-conformance-park-signal
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite | PASS | 428/428 workspace tests; was 418 before this spec — 10 new tests added |
| AC coverage | 4/4 | See report below |
| `orbit verify` | PASS | clean on this repo |
| End-to-end smoke | PASS | Fixture with parked + non-parked card produces single finding for the non-parked card; `orbit verify` clean against the same fixture |

## AC Coverage Report

| AC | `ac_type` | Status | Evidence |
|----|-----------|--------|----------|
| ac-01 | code | ✓ | `park_signal_fields_matches_struct`, `card_round_trips_without_park`, `card_round_trips_with_park`, `card_rejects_park_with_empty_reason`, `card_rejects_park_with_empty_until`, `card_rejects_park_missing_until`, `card_rejects_park_with_unknown_subfield` (schema.rs:889-1008); plus ac-01 (f) audit-drift no-loosening confirmed via end-to-end probe (parked card with top-level unknown field still drifts) |
| ac-02 | code | ✓ | `conformance_card_state_skips_parked_card`, `conformance_card_state_park_only_affects_card_state_family`, `conformance_card_state_park_irrelevant_when_emerging`, `conformance_card_state_park_irrelevant_with_specs` (verbs.rs:10023-10100); `audit_conformance_cli_park_signal_envelope` (cli/tests/parity.rs:556); `audit_conformance_mcp_park_signal_envelope` (mcp/tests/parity.rs:266) |
| ac-03 | doc | ✓ | plugins/orb/skills/card/SKILL.md:114-136 "Parking a card" section — all four required claims (when, reason: shape, until: shape, how to unpark) plus scope ("card-state finding family") and v1 limitation present; no out-of-scope automated-unpark claim |
| ac-04 | doc | ✓ | plugins/orb/skills/setup/SKILL.md:251 "Parked-card carve-out" paragraph — names "card-state family", "ready_for_design", "no envelope trace", cross-links to `/orb:card`'s *Parking a card* section; no contradictory severity/aggregated-count claim |

## Findings

### [INFO] All four ACs satisfied with evidence-shaped tests

**Category:** test-coverage
**Description:** Every code-type AC has a directly-named test (`fn name` mirrors the AC's required behaviour); every doc-type AC has grep-confirmable prose in the named file. Type-keyed evidence expectations apply cleanly — no false positives from doc-typed ACs flagged as missing tests.
**Evidence:** See AC Coverage Report.
**Recommendation:** none.

### [INFO] End-to-end smoke confirms behaviour

**Category:** edge-case
**Description:** Built a fresh `/tmp` fixture with one parked card (`park: {reason, until}`) and one non-parked planned-empty card. `orbit --json audit conformance` returns exactly one `ready_for_design` finding for the non-parked card; the parked card produces zero envelope trace. `orbit verify` is clean against the same fixture (confirms ac-01 (f) — Card::FIELDS extension does not loosen the unknown-field check; an injected top-level unknown field still surfaces in `audit drift`).
**Evidence:** `/tmp/park-signal-smoke` + `/tmp/park-probe-unknown` probes during this review.
**Recommendation:** none.

### [INFO] Memory dispositions match the implementation shape

**Category:** scope
**Description:** Three memories considered in the spec — `spec-close-does-not-bump-card-maturity` (partially-adopted, shape-1 fix deferred), `feedback-card-vs-choice-distinction` (adopted), `design-implementation-filter-on-envelope-shape` (adopted, silent envelope). The implementation respects all three: shape 1 (stale-field) is untouched, no MADR record was created (extension lives in the spec), envelope is silent for parked cards.
**Evidence:** spec.yaml `memories_considered` block + verbs.rs:3802 (`continue` with no finding push).
**Recommendation:** none.

### [INFO] No choice records exist for the carve-out

**Category:** scope
**Description:** The spec's goal names the mechanism choice ("explicit-schema-field path... chosen over a parked maturity value and over a notes-prefix convention") but the spec explicitly routes this to live in the spec rather than a separate MADR (memory disposition: "borderline choice-shaped but small enough"). No follow-up needed.
**Evidence:** `feedback-card-vs-choice-distinction` memory disposition in spec.yaml; choice-keyword scan finds no `.orbit/choices/*.yaml` recording the carve-out mechanism.
**Recommendation:** none.

---

## Honest Assessment

A tight, well-bounded carve-out. The implementation matches the spec line-for-line: a small `ParkSignal` struct with `deny_unknown_fields` + a `non_empty_string` deserialize helper, one extra clause in the `card_state_findings` walk (`|| card.park.is_some()`), and prose in two skill files. Test coverage is dense — every AC has a directly-named test or a grep-confirmable prose block, and the symmetric empty-until test (added per the review-spec MINOR finding) closes the asymmetry the spec called out in ac-01 (c). CLI + MCP parity tests close the surface-divergence risk on the audit envelope. The end-to-end smoke run on a fresh fixture confirmed the silent-envelope contract and the audit-drift no-loosening claim — both behaviours that unit tests can attest but only end-to-end can prove. Shape 1 (stale-field) remains an open false-positive source on this repo's audit run (visible in the live `orbit audit conformance` output — multiple `planned`-empty-specs cards still fire), but that's correctly out of scope per the spec's memory disposition; a separate spec will own it. No blockers; merge.
