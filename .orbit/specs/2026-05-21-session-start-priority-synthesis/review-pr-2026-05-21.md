# Pre-Merge Review

**Date:** 2026-05-21
**Reviewer:** Context-separated agent (fresh session)
**Branch:** main (uncommitted working tree)
**Spec:** 2026-05-21-session-start-priority-synthesis
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite | N/A | Prose-only skill — no executable surface to run. `orbit verify` clean. |
| AC coverage | 9/9 (doc-typed) | All ACs are documentation contracts on a SKILL.md; coverage table below. |
| Substrate hygiene | PASS | `orbit verify: clean` |
| Conformance | NO-REGRESSION | 4 pre-existing MEDIUM findings — all `cards/ready_for_design`, none introduced by this change. Card 0043 itself is one of them (specs:[] until close). |

## AC Coverage Report

This spec ships a single SKILL.md (`plugins/orb/skills/prioritise/SKILL.md`). Every AC describes a prose contract the file must encode — there is no test-bearing code surface. Under the type-keyed evidence table in `/orb:review-pr` these are `doc`-band ACs (closure = grep + content check on the named artefact), not `code`-band. The walk below cites SKILL.md line ranges as the evidence anchor.

| AC | Status | Evidence in SKILL.md |
|----|--------|----------------------|
| ac-01 | PASS | L11 ("top 5 priorities"), L54 ("Top 5 only. Overflow surfaces as a count line, never enumerated"), L58-65 (4-line per-priority shape — what/why/effort/next), L67-69 (S/M/L scale defined exactly as spec'd) |
| ac-02 | PASS | L19 ("session start or any time mid-session"), L29-33 (three live inputs: conformance / session prime / recent memories), L21-25 ("Session handovers freeze priorities … this skill re-derives the same synthesis live"). All three referenced verbs confirmed present in `orbit --help` (verified inline). |
| ac-03 | PASS | L11 ("never auto-executes"), L93-95 ("Not auto-executing. Enforcement is via this SKILL.md's prose contract — there is no hook or tool-permission gate. The skill matches existing read-only surfaces like `orbit overview`"), L100 ("Read-only. No `orbit task open`, no `orbit memory remember`, no `orbit spec note`. The skill never writes."). |
| ac-04 | PASS | L44-48: "surface its `remediation.verb` verbatim — no translation, no paraphrase" + "When a finding lacks `remediation.verb` … surface the finding's `evidence` field verbatim and tag the item `manual action`". Verbatim-tagging exactly as the AC text demands. |
| ac-05 | PASS | L92-94 ("Not `orbit overview`. That verb produces a status snapshot … This skill produces a ranked ACTION plan keyed off the conformance findings + session envelope"). Correctly drops the `/orb:handover` neighbour (card 0036 hasn't shipped — cycle-2 review-spec already flagged and resolved this). |
| ac-06 | PASS | L71 ("Total brief: **≤20 lines or ~500 tokens**. Cite memory keys, conformance subjects, and file paths the author can open on demand — do NOT inline substrate content"), reinforced at L102 ("Cite, don't paraphrase"). |
| ac-07 | PASS | File exists at `plugins/orb/skills/prioritise/SKILL.md` (verified). Frontmatter `name: prioritise`, `user-invocable: true` (L2, L4). H1 `# /orb:prioritise` at L9. |
| ac-08 | PASS | L37-42: four-tier deterministic ranking — severity HIGH>MEDIUM>LOW, then memo staleness days desc, then open-spec age desc, ties by id ascending. L37 explicitly invokes "byte-identical ordering" — falsifiable property exactly as ac-08 demands. |
| ac-09 | PASS | L73-80: empty-substrate fallback — first surface `maturity: planned` + `specs:[]` cards (uses `orbit overview` to list them), then explicit string `no priorities — substrate is clean and the backlog is exhausted`. L80: "Never return an empty brief without an explanation." |

## Diff Inventory

Six working-tree changes; one is the implementation artefact, the rest are workflow substrate.

| Path | Kind | Disposition |
|---|---|---|
| `plugins/orb/skills/prioritise/SKILL.md` | new | The implementation. Single new SKILL.md, 103 lines. |
| `.orbit/cards/0043-session-start-priority-synthesis.yaml` | new | Card created by the drive (`maturity: planned`, `specs: []`). |
| `.orbit/specs/2026-05-21-session-start-priority-synthesis/` | new | Spec + cycle-1 review-spec REQUEST_CHANGES + cycle-2 review-spec APPROVE + drive.yaml + notes.jsonl. |
| `.orbit/memos/2026-05-19-prioritise-skill.md` | deleted | Source memo correctly retired post-promotion to card 0043 (card's `references:` field still points at it per the convention — see note below). |
| `.orbit/memories/distill-source-lines-vs-schema.yaml` | new | Side-discovery memory captured during the drive — `source_lines` field rejected by Card schema; unrelated to this spec's surface. Carry forward. |
| `.orbit/sessions/cfb21f04-7924-4929-bdd2-366203c31388.yaml` | new | Stop-hook distillate. Bookkeeping. |

## Findings

### [LOW] Card `references:` points at a deleted memo

**Category:** edge-case
**Description:** `.orbit/cards/0043-session-start-priority-synthesis.yaml` line 51 lists `.orbit/memos/2026-05-19-prioritise-skill.md` under `references:`, but the diff deletes that memo. The reference becomes a dangling pointer at merge time.
**Evidence:**
- `git diff` shows `.orbit/memos/2026-05-19-prioritise-skill.md | 26 --------------------------` (full deletion).
- `.orbit/cards/0043-session-start-priority-synthesis.yaml:50-51`: `references:\n- .orbit/memos/2026-05-19-prioritise-skill.md`.
**Recommendation:** Optional. The METHOD.md vocabulary table for memos reads "Deleted after promotion to a card" — so the memo deletion is correct. The dangling `references:` entry is the cost. Two options: (a) clear the `references:` list now that the source is gone; (b) leave it as historical provenance — the path encodes when the idea entered the substrate even after the file is gone. Card 0041 (`reference-integrity`) is the eventual home for this question; flag for that card's spec, do not block this merge.

### [LOW] Untracked side-files not part of this spec

**Category:** environment-mismatch
**Description:** Two untracked files (`distill-source-lines-vs-schema.yaml` memory, session yaml) ship alongside the implementation. They're legitimate substrate accretions but they aren't part of this spec's ACs. Reviewer should know they exist and not mistake them for spec deliverables.
**Evidence:** `git status` shows them as untracked alongside the spec folder.
**Recommendation:** Carry forward — the distill memory captures a real schema/SKILL drift worth feeding into a `/orb:distill` SKILL.md fix later. No action required on this merge.

---

## Honest Assessment

This is a prose-only skill — one SKILL.md and the card+spec scaffolding the drive produced. There is no test surface to run, by design (ac-03 makes prose the enforcement layer). Every AC is a `doc`-band evidence check against the SKILL.md, and all nine pass on a line-by-line read.

The implementation is tight in the places that matter most. The ranking algorithm in ac-08 (the falsifiable test surface) is pinned with the byte-identical-output guarantee in the SKILL.md prose (L37) and matches the four-tier order the spec mandates. The output shape in ac-01 / ac-06 is concretely bounded (top 5, 4-line per item, ≤20 lines / ~500 tokens) — the reviewer can count lines against the prescribed shape. The differentiation against `orbit overview` in ac-05 is sharp (status-snapshot vs ranked-action) and correctly stops short of comparing against card 0036's `/orb:handover` since that capability hasn't shipped yet — cycle-2 review-spec called this out and the implementation honoured it.

Two LOW findings are both adjacency rather than spec gaps. The dangling memo reference in card 0043 is the price METHOD.md pays for its "delete memo after promotion" rule — it's a substrate-wide question that belongs in card 0041's spec, not here. The side-files are real workflow output but don't belong on the AC list.

The verdict-line contract is satisfied at the top of this file. No revisions required before merge.

**Verdict:** APPROVE
