# Pre-Merge Review

**Date:** 2026-05-25
**Reviewer:** Context-separated agent (fresh session)
**Branch:** feat/2026-05-25-port-setup-method-sh
**Spec:** 2026-05-25-port-setup-method-sh
**Verdict:** APPROVE

---

## Test Results

| Check          | Result | Details                                                            |
|----------------|--------|--------------------------------------------------------------------|
| Test suite     | PASS   | 554/554 across 8 suites (1.95s); +15 vs. main matches changelog    |
| `cargo build`  | PASS   | 0 errors, 1 pre-existing dead-code warning in `bin/migrate.rs`     |
| `orbit verify` | PASS   | clean (no drift)                                                   |
| `setup_files` parity tests | PASS | 15/15 (8 CLI + 7 MCP) covering all ac-06 scenarios + idempotency |
| AC coverage    | 11/11  | See report below                                                   |

## AC Coverage Report

| AC    | `ac_type` | `gate` | Evidence shape per type-keyed expectations | Status |
|-------|-----------|--------|----|----|
| ac-01 | code      | true   | `SetupFilesArgs`/`SetupFilesResult` land at `crates/core/src/verbs.rs:3083` with `LegacyAction`/`DriftAction` enums (kebab-case-serialised) at lines 24-44 of the diff; default canonical path resolves via `ORBIT_PLUGIN_ROOT` (`verbs.rs:3198`). Parity tests exercise the struct round-trip via every scenario. | ✓ |
| ac-02 | code      | false  | CLI `build_setup_files_request` (`crates/cli/src/main.rs:2225-2300`) parses `--answer-{legacy,method-drift,style-drift}` then prompts via stdin only when answer is absent AND needed. Core verb (`verbs.rs:3083`) takes typed enums; no stdin awareness. MCP layer (`crates/mcp/src/main.rs:291-310`) declares `inputSchema` with enums; all 7 MCP tests pass typed args. | ✓ |
| ac-03 | code      | false  | `LegacyAction::Refuse` branch (`verbs.rs:3131-3136`) returns `Error::conflict("setup.files", "legacy CLAUDE.md blocks present; legacy_action=refuse halts setup — no files copied, no @-imports added")` — verbatim match. `LegacyAction::Migrate` branch uses line-walking strip (`strip_legacy_sections`, no regex lookahead), `collapse_blank_runs` (3-plus → 2), then overwrite copies + import-ensure. CLI test `setup_files_cli_legacy_refuse_errors_and_writes_nothing` takes a filesystem snapshot before/after refuse and asserts equality. | ✓ |
| ac-04 | code      | false  | `copy_canonical_with_drift` (`verbs.rs:3334-3366`) uses `std::fs::read` bytewise equality (not `same_file`), returns `FileAction::Identical` on match, otherwise applies the supplied `DriftAction`. CLI's `resolve_drift_action` (`main.rs:2308-2346`) only prompts when destination exists AND differs. Four parity tests (method overwrite/keep + style overwrite/keep) cover the matrix. | ✓ |
| ac-05 | code      | false  | `ensure_at_imports` (`verbs.rs:3371-3429`) creates CLAUDE.md from scratch with both imports when missing, otherwise full-line scans (`text.lines().any(|line| line == *import)`) and appends with blank-line padding. The idempotency test (`setup_files_cli_idempotent_filesystem_byte_equality`) asserts on-disk CLAUDE.md/METHOD.md/STYLE.md byte-equality after running the verb twice on the same greenfield project. | ✓ |
| ac-06 | code      | true   | 15 parity tests at `crates/cli/tests/parity.rs:2046-2228` and `crates/mcp/tests/parity.rs:1028-1124`. All 7 fixture scenarios named in the spec are covered: greenfield (a), legacy-migrate (b), legacy-refuse with filesystem-snapshot equality (c), method drift overwrite (d), method drift keep (e), style drift overwrite (f), style drift keep (g). Shared fixtures `populate_setup_files_*` in `cli/tests/common/mod.rs:1056-1089`. | ✓ |
| ac-07 | code      | true   | `plugins/orb/skills/setup/SKILL.md` line 246 rewritten to `orbit setup files --project-root <project>`. `rg --no-heading 'setup-method\.sh' plugins/orb/skills/` returns zero. (Pre-flight count was 1 SKILL.md call site, matching spec.) | ✓ |
| ac-08 | code      | false  | Commit `4703b2b` deletes `plugins/orb/scripts/setup-method.sh` (244 lines) AND `plugins/orb/scripts/tests/test-setup-method.sh` (325 lines) in the same commit that rewrites the SKILL.md call site. No orphaned wrapper. | ✓ |
| ac-09 | doc       | false  | Choice 0020's "in-scope migration candidates" table — `setup-method.sh` row marked `**MIGRATED** in spec 2026-05-25-port-setup-method-sh` with strike-through on the original cell. `date_modified: 2026-05-25` set. Rest of choice file untouched (1-line diff). | ✓ |
| ac-10 | code      | false  | `.orbit/cards/0017-setup-is-orbit-state-aware.yaml` gains a `relations` entry with `choice: '0020'`, `type: respects`, and the spec-supplied reason verbatim. `orbit verify` clean confirms canonicalisation passed. | ✓ |
| ac-11 | code      | false  | `cargo test --workspace` passes 554 tests. `orbit verify` clean. Workspace `version` bumped 0.4.36 → 0.4.37 (`orbit-state/Cargo.toml:9`). `CHANGELOG.md` entry under `[0.4.37] - 2026-05-25` names the verb, the CLI prompt layer, the shim + test deletions, the choice 0020 table update, and the card 0017 relations edge. | ✓ |

## Findings

None. Implementation matches the spec contract clause-for-clause:

- The `Error::conflict` message for legacy-refuse is byte-identical to the spec text (including the em-dash).
- The line-walking marker scan (`strip_legacy_sections`) discharges spec ac-03's "implementer's choice" allowance — RE2 lookahead-free as the spec required.
- Filesystem-snapshot equality on the legacy-refuse path is asserted in the CLI parity test, satisfying the spec's "no disk writes" hard-stop.
- The idempotency test asserts on-disk byte-equality (not merely envelope no-op), satisfying spec ac-06's "load-bearing assertion" demand.
- The shim deletion + SKILL.md rewrite + choice 0020 table update + card 0017 relations edge all land in the same commit (`4703b2b`), discharging ac-08's no-orphaned-wrapper invariant.

Audit conformance run surfaces only pre-existing stale-memo findings (`.orbit/memos/2026-05-16-*` and `2026-05-17-*`) and an unrelated `pin.status: unpinned` — unchanged by this PR, out of scope for this review.

## Honest Assessment

This is a textbook execution of a spec contract. All eleven ACs map to either a test (the `code` ACs) or a verifiable file edit (ac-09 doc), with no untyped ACs to interpret. The three-cycle spec-review iteration (REQUEST_CHANGES → REQUEST_CHANGES → APPROVE) shows visible tightening of the AC contract before code, which pays off here: there's nothing to argue about. The implementation respects choice 0020's no-orphaned-wrapper invariant by deleting the shim and its dedicated test in the same commit as the call-site rewrite, and discharges the deferred relations edge promise on card 0017 that PRs #32 + #33 + #34 had been progressively unblocking. The test count (539 → 554, +15) matches the changelog's claim exactly. Approve and merge.
