# Pre-Merge Review

**Date:** 2026-05-08
**Reviewer:** Context-separated agent (fresh session)
**Branch:** four-pillars-wires
**Spec:** 2026-05-08-four-pillars-wires
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| `orbit verify` | PASS | clean |
| `orbit-state` cargo tests | PASS | 136 passed across 8 suites |
| `test-promote-gate-propagation.sh` | PASS | gate marker propagation verified end-to-end |
| `test-gate-ac-verification.sh` | PASS | gate-AC rules behave as expected |
| AC coverage | 0/7 (doc-only spec) | see report below |

## AC Coverage Report

| AC | Status | Test(s) |
|----|--------|---------|
| ac-01 | n/a | NO TEST FOUND — README rename, doc-only |
| ac-02 | n/a | NO TEST FOUND — card scenario amendment, doc-only |
| ac-03 | n/a | NO TEST FOUND — card scenario amendment, doc-only |
| ac-04 | n/a | NO TEST FOUND — card scenario amendment, doc-only |
| ac-05 | n/a | NO TEST FOUND — card references-list edit, doc-only |
| ac-06 | gate | `orbit verify` clean; `orbit spec close` deferred to drive completion |
| ac-07 | n/a | NO TEST FOUND — relations-graph edits, verified by inspection |

This spec is documentation-only: a README heading rename, six card-scenario / relations / references edits on `.orbit/cards/0028-four-pillars.yaml`, and a spec file (`2026-05-08-four-pillars-wires.yaml`) plus drive state. There is no executable contract to test. AC-06 is the one gate that touches tooling (`orbit verify` and `orbit spec close`) — `orbit verify` runs clean; `orbit spec close` is invoked by drive at drive completion, not at PR time, and the spec is correctly still `status: open`.

## Findings

### [LOW] Card 0028's `goal` field still reads as the pre-amendment contract
**Category:** doc-drift
**Description:** The card's top-level `goal:` (line 5 of `.orbit/cards/0028-four-pillars.yaml`) still reads `every card carries a pillar field naming at least one pillar; cards that move no pillar are re-scoped or retired; distill / design cannot pass without a pillar claim`. The amended scenarios (ac-02 through ac-04) explicitly drop the schema-field requirement, drop the distill pillar question, and drop the design / review-spec pillar surfacing. The `goal:` text now contradicts the realised contract.
**Evidence:** `/home/hugh/github/hughcameron/orbit/.orbit/cards/0028-four-pillars.yaml:5` retains the original `goal:`; `scenarios[1]`, `scenarios[3]`, `scenarios[4]` and `scenarios[7]` all reframed for documentation-only wiring per ac-02/03/04.
**Recommendation:** Out of scope for this PR — the spec's ACs target `scenarios:`, `relations:`, and `references:` only. File a follow-up spec (or a small doc fix on the next drive) to align the `goal:` field with the amended scenarios. Suggested: "every card may declare pillar contribution via a `relations: feeds` edge to a pillar-defining card; pillar awareness informs agent shaping silently — not a required schema field, not surfaced as an interview question". Not blocking.

### [INFO] AC-06 second clause completes at drive close, not PR merge
**Category:** sequencing
**Description:** AC-06 claims `orbit spec close 2026-05-08-four-pillars-wires` succeeds at drive completion and Choice 0019's audit signal flips for card 0028 from aspirational (`specs: []`) to wired. At PR review time card 0028 still shows `specs: []` because the close has not yet run. This is correct sequencing under the drive contract — the close is post-merge — but the AC is checked `[x]` before the action it predicts has occurred. Reviewer judgement: acceptable, because the spec's `[x]` reflects implementer commitment to invoke the close, and `orbit verify` clean is the merge gate.
**Evidence:** `grep -E "^specs:" .orbit/cards/0028-four-pillars.yaml` → `specs: []`; `orbit --json spec show ...` → `status: open`.
**Recommendation:** No action — drive completion will execute the close. The reviewer notes the timing for transparency.

---

## Honest Assessment

The diff is small, surgical, and respects choice 0019. README's H2 is renamed in place (no parallel section), card 0028's gate scenarios 2 and 8 are reframed for documentation-only wiring (no schema field, no audit pillar-check), the non-gate scenarios 4 and 5 are reframed as silent agent-side awareness, the stale memo reference is moved to `historical:` form mirroring 0026's precedent, and the relations block carries `feeds` edges covering all four pillars with reason text that names the pillar served. `orbit verify` runs clean and the existing test suite (136 cargo tests + two shell tests for orbit-state's gate-AC tooling) is unaffected. Choice 0019 is honoured: no new schema field, the wires are named in the gate scenario, and the `specs:` array remains the load-bearing diagnostic — once drive close lands, card 0028 flips from aspirational (`specs: []`) to wired. The one drift I found — the card's top-level `goal:` still describing the pre-amendment contract — is real but out of scope for this spec's ACs and easily addressed in a follow-up. There are no test gaps because there is no code change; the spec is a documentation amendment and is verified by inspection plus `orbit verify`.
