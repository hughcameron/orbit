# Pre-Merge Review

**Date:** 2026-05-22
**Reviewer:** Context-separated agent (fresh session)
**Branch:** feat/2026-05-22-routine-proposals
**Spec:** 2026-05-22-routine-proposals
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| `cargo build --workspace` | PASS | 0 errors, 1 pre-existing warning (unused `BdRecord` methods in `migrate.rs`, not from this branch) |
| `cargo test --workspace` | PASS | 474 tests across 8 suites in 2.19s |
| `cargo test routine_ac` | PASS | 18 / 18 AC-keyed tests pass |
| `cargo test routine::` | PASS | 10 / 10 inner-module tests pass |
| `cargo clippy --workspace --all-targets` | PASS | 0 errors, 14 warnings (all stylistic, none in load-bearing routine paths) |
| AC coverage | 9 / 9 closed ACs | ac-10 is `ac_type: observation` and legitimately defers |
| Choices keyword scan | Clean | No relevant choice records constrain routine substrate |

## AC Coverage Report

| AC | `ac_type` | Status | Test(s) / Evidence |
|----|-----------|--------|---------------------|
| ac-01 | code (default) | PASS | `routine_ac_01_chain_reconstruction_via_aggregator` — verifies the aggregator-verb path (option b), `routine.chains` reconstructs one ordered chain per `session_id` from `.orbit/skills/*.invocations.jsonl` rows, no schema change |
| ac-02 | code | PASS | Four tests — `routine_ac_02_exact_match_length_two`, `_length_three_allows_one_skipped_step`, `_longest_chain_wins`, `_single_session_does_not_trigger`. All three matching-rule sub-cases (a)/(b)/(c) covered |
| ac-03 | code | PASS | `routine_ac_03_author_writes_skill_md` — verifies SKILL.md lands at `.claude/skills/<name>/SKILL.md` with chain body. No proposal artefact, no AskUserQuestion gate (write is direct) |
| ac-04 | config | PASS | Three tests — `routine_ac_04_chain_id_matches_canonical_example` (SHA-256 of canonical JSON for the worked example), `_front_matter_validates_on_write`, `_rejects_short_chain`. Also `chain_id_matches_canonical_example` in `routine.rs` unit tests confirms `chain_id(["/orb:tabletop","/orb:spec","/orb:implement"])` is the SHA-256 of `["/orb:tabletop","/orb:spec","/orb:implement"]` byte sequence |
| ac-05 | code | PASS | `routine_ac_05_fan_out_pattern_does_not_trigger_authoring` — fan-out shape recorded in invocation stream but not surfaced by `detect_recurring_chains` |
| ac-06 | code | PASS | Three tests — `_verify_advances_last_verified_on_pass`, `_verify_does_not_advance_when_ref_broken`, `_audit_is_read_only_byte_equal_across_runs`. The byte-equal-across-runs test directly verifies AC-06 verification clause (b) |
| ac-07 | code | PASS | `routine_ac_07_stale_finding_at_thirty_days` + `_broken_refs_finding`. Both state slugs covered; subsystem slug `routines`, severity `medium`, remediation verbs `orbit routine verify <path>` and `archive via curator` match the AC verbatim |
| ac-08 | code | PASS (partial doc gap — see below) | `routine_ac_08_no_cross_imports_with_skill_author` — code-search test asserts no `skill_author` reference in either `routine.rs` or the verb block in `verbs.rs` (comments stripped to avoid false positives) |
| ac-09 | code | PASS | `_archived_chain_is_not_reauthored` + `_renamed_directory_still_matches`. Both fixture scenarios from the AC are covered — content-addressed (path-independent) dedupe verified |
| ac-10 | observation | DEFERRED | Correctly deferred per `ac_type: observation` band — closes via post-ship soak |

## Findings

### [LOW] AC-08 cross-reference present only in card 0013, not in card 0022
**Category:** test-gap (doc-level)
**Description:** AC-08 says "the boundary is documented in both card 0013 and card 0022 references". Card `0013-routine-proposals.yaml` carries the cross-references (lines 6, 19, 22, 36) but `0022-skill-curator.yaml` has no reference to card 0013 or to the routine substrate. The code-search half of AC-08 is fully covered by `routine_ac_08_no_cross_imports_with_skill_author`; the documentation half is one-sided.
**Evidence:** `grep -nE 'card 0013|0013-routine' .orbit/cards/0022-skill-curator.yaml` returns no matches. `grep -nE 'card 0022|0022-skill' .orbit/cards/0013-routine-proposals.yaml` returns 4 matches.
**Recommendation:** Add a `relations` entry on card 0022 pointing at card 0013 (or a `references` line naming the routine substrate boundary). Low severity because the load-bearing code-boundary check is enforced by a test, and one-sided cross-references are still navigable.

### [LOW] Clippy stylistic lints on routine substrate (cosmetic)
**Category:** performance / style
**Description:** Two clippy hints touch new code in `routine.rs` — `sort_by_key` over `sort_by` at line 305, and `split_once` over manual splitn at line 589. Neither affects correctness.
**Evidence:** `cargo clippy --workspace --all-targets` output.
**Recommendation:** Optional polish — fold into a follow-up substrate-hygiene pass rather than blocking merge.

### [INFO] AC-03 commit-message claim is contract, not enforced by test
**Category:** documentation
**Description:** AC-03 names a commit-message format ("feat(skills) draft .claude/skills/<name> from recurring chain — N occurrences"). The `routine.author` verb returns `chain_id`, `name`, `path`, and accepts an `occurrences` arg the caller surfaces into the commit message; the verb itself does not commit. The contract lives at the calling agent's layer.
**Evidence:** `routine_author` in `verbs.rs` (lines 5376+) writes the file and returns the result envelope; commit-message construction is the agent's responsibility.
**Recommendation:** No change — the split is correct (substrate writes the file; agent composes the commit). Noted so a future review of the agent prose / runbook can verify the commit-message format is followed once the first real recurring chain triggers authoring.

---

## Honest Assessment

This is a clean implementation. All nine closing-band ACs have dedicated tests with naming that maps 1:1 to the AC ids; the substrate splits responsibilities cleanly along the boundaries the spec named (aggregator vs schema-change for AC-01; verify-verb writes vs audit-read for AC-06; content-addressed dedupe for AC-09). The choice to render front-matter manually rather than via `serde_yaml::to_string` keeps the byte-stable ordering AC-06 leans on, and the `routine.author` content-addressed early-return (lines 379-389 in the diff) is exactly the AC-09 invariant the substrate needed. Test count is 474 across the workspace, and the new substrate (chain reconstruction, recurrence detection, front-matter parse / render / validate, verify, audit findings) is comprehensively covered. The one substantive gap is the half-missing cross-reference on card 0022; it's low severity because the code-boundary check is test-enforced and one-sided cross-references still navigate. Merge.
