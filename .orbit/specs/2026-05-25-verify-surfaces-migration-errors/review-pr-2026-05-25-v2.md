# Pre-Merge Review (cycle 2)

**Date:** 2026-05-25
**Reviewer:** Context-separated agent (fresh session)
**Branch:** drive/verify-surfaces-migration-errors
**Spec:** 2026-05-25-verify-surfaces-migration-errors
**Cycle:** 2 (cycle 1 verdict: REQUEST_CHANGES — test-prefix mismatch)

**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| `cargo test --workspace` | PASS | 555 passed, 0 failed (≥510 required; ac-03 satisfied) |
| `cargo test --workspace verfy` | PASS | 1 passed, 554 filtered out (ac-02 prefix wired correctly) |

---

## AC Coverage Report

| AC | Type | Status | Evidence |
|----|------|--------|----------|
| ac-01 | code | ✓ | `let _ = ensure_current(layout)` discard gone; match arm at verify.rs:98 pushes `RoundTripFailure { path: layout.schema_version_file(), kind: ParseFailed("schema-version unrunnable: {e}") }` on Err; Ok arm is no-op |
| ac-02 | code | ✓ | `fn verfy_surfaces_unrunnable_schema_version_as_round_trip_failure` present; writes `version: '0.99'`, calls `verify_all`, asserts `has_failures()` true and a `ParseFailed` entry on the schema-version path; `cargo test --workspace verfy` returns 1 passed |
| ac-03 | code | ✓ | 555 tests green; zero failures; no pre-existing test turned red |

Coverage: 3/3 ACs satisfied.

---

## Cycle-1 Finding Resolution

The single REQUEST_CHANGES item from cycle 1 — test function named `verify_surfaces_unrunnable_schema_version_as_round_trip_failure` (`verify_` prefix) instead of `verfy_` — is resolved. The function now carries the `verfy_` prefix exactly as AC-02 specified and as the spec goal text declared. `cargo test --workspace verfy` confirms the test is reachable by the spec-scoped filter.

---

## Implementation Quality

The fix is minimal and correct. The `ensure_current` error path is caught, wrapped as a synthetic `RoundTripFailure::ParseFailed` on `layout.schema_version_file()`, and pushed into `outcome.round_trip_failures` before the remainder of `verify_all` runs. The wrapped message surfaces the migration runner's Display output directly — operator-readable prose naming the unknown version and the known-version list. The Ok arm is a no-op, preserving the happy-path shape.

The observation from cycle 1 stands (not blocking): execution continues past the synthetic failure to `check_round_trip::<SchemaVersion>`, where `version: '0.99'` deserialises as a plain string and passes. The file therefore appears both as ParseFailed in `round_trip_failures` and as passing the round-trip check. This is benign — `has_failures()` is true, the operator sees the diagnostic — and the tabletop explicitly accepted synthetic-error reuse over a dedicated `VerifyOutcome.migration_failures` field. No action required unless future callers enumerate per-file pass/fail status.

---

## Merge Recommendation

Merge. All three ACs are satisfied, the test suite is green at 555, and the cycle-1 defect is closed.
