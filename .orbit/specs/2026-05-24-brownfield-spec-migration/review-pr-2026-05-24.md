# Pre-Merge Review

**Date:** 2026-05-24
**Reviewer:** Context-separated agent (fresh session)
**Branch:** rally/brownfield-spec-migration
**Spec:** 2026-05-24-brownfield-spec-migration
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite (`cargo test`, workspace) | PASS | 509 passed, 0 failed (8 suites) |
| Reconcile module tests (focused) | PASS | 44 passed |
| Clippy (`cargo clippy --all-targets -D warnings`) | PASS for this diff | 8 errors in workspace but all pre-existing on main (verbs.rs, routine.rs, locks.rs, migrate.rs); none in reconcile.rs |
| AC coverage | 10/10 | See report below |

## AC Coverage Report

This drive's contracted scope (per `decisions.md`) is ac-08, ac-09, ac-10 — the legacy maturity rule. ac-01..ac-07 were closed by prior in-tree work (`2026-05-12-reconcile-mode`, `2026-05-21-richer-reconcile-rules`) and were verified intact by this review.

| AC | Status | Evidence |
|----|--------|----------|
| ac-01 (`code`, gate) | covered (prior work) | dry-run + non-dry-run paths shipped pre-drive; strict-parse invariant still in `orbit-state/crates/core/src/schema.rs`; 509-test workspace pass exercises the verb. |
| ac-02 (`code`, gate) | covered (prior work) | `<name>.legacy.yaml` sidecar quarantine path in `reconcile.rs` exercised by the unknown-key tests already in the module (e.g. `card_scenarios_inner_field_walked`). |
| ac-03 (`code`) | covered (prior work) | `Map`/`Drop`/`Synthesise`/`WrapListElement` covered by tests in the `2026-05-21-richer-reconcile-rules` family. |
| ac-04 (`code`, gate) | covered (prior work) | No permissive-mode escape hatch added; `schema.rs` untouched in this diff. |
| ac-05 (`code`) | covered (prior work) | `orbit verify`'s wiring unchanged in this diff; `--reconcile` remains an opt-in mode on `canonicalise`. |
| ac-06 (`code`) | covered (prior work) | `FIELD_RULES` const + per-handler shape is the discoverable registry; the new entry follows that shape one-for-one. |
| ac-07 (`code`, gate) | covered (prior work) | `plugins/orb/skills/setup/SKILL.md:154,160,162` invokes `orbit canonicalise --reconcile`; `.orbit/cards/0030-canonical-schema-doc.yaml:42` names the mode. |
| ac-08 (`code`, gate) | covered (this drive) | `reconcile_card_maturity` defined at `reconcile.rs:447-493`; FIELD_RULES entry at `reconcile.rs:282-286`; tests `card_maturity_legacy_active_maps_to_established`, `card_maturity_legacy_in_design_maps_to_emerging`. |
| ac-09 (`code`) | covered (this drive) | Four-outcome routing tested by `card_maturity_legacy_*`, `card_maturity_canonical_value_passes_through`, `card_maturity_unknown_value_quarantines_at_handler_level`, `card_maturity_non_string_value_quarantines`. Detail strings asserted verbatim. |
| ac-10 (`code`, gate) | covered (this drive) | `apply_top_level_transform` defined at `reconcile.rs:1104-1148`; walker integration at `reconcile.rs:794-810`; the pre-existing `card_scenarios_inner_field_walked` test now asserts the second disposition (maturity pass-through) per the AC's verifiability clause (test edit at `reconcile.rs:1494-1517` of the diff). |

Coverage: 10/10 ACs have evidence (100%).

Cross-language test-naming note: orbit-state's test convention is descriptive function names, not `acNN` prefixes — confirmed by greping the whole crate, which yields zero `fn ac<NN>` / `fn test_ac<NN>` matches across both this drive's ACs and the prior `2026-05-21-richer-reconcile-rules` ACs. The mapping from AC to test is carried in comments (`// 2026-05-24-brownfield-spec-migration ac-08`, etc.) and in test names whose semantics make the routing unambiguous. Treating the missing `acNN` prefix as a gap would be a coverage-report false positive on this codebase.

## Findings

None at REQUEST_CHANGES or BLOCK severity.

### [INFO] Workspace-wide clippy debt pre-dates this drive
**Category:** environment-mismatch (advisory)
**Description:** `cargo clippy --all-targets -D warnings` reports 8 errors and 1 warning across `verbs.rs`, `routine.rs`, `locks.rs`, `migrate.rs`. None touch `reconcile.rs` or `schema.rs`. Verified by `git stash`-ing the working tree (no local changes existed, so this was equivalent to running clippy on the branch HEAD) and comparing — the same warnings appear on `main`'s tip without this drive's diff applied.
**Evidence:** Stash-test output and the diff scope showing reconcile.rs is the only touched source file.
**Recommendation:** Out of scope for this review — flag for a separate housekeeping memo or spec. Does not gate merge.

### [INFO] One drive-state sidecar file in the diff is a working artefact
**Category:** environment-mismatch (advisory)
**Description:** `.orbit/specs/2026-05-24-brownfield-spec-migration/drive.yaml` is a 10-line drive-state sidecar created by `/orb:drive`. This is expected substrate state from a drive — not a code change — and does not affect the review verdict.
**Evidence:** `git diff main...HEAD --stat` shows the three changed paths: `drive.yaml`, `spec.yaml`, `reconcile.rs`.
**Recommendation:** None. Include or strip per repo norm.

---

## Honest Assessment

This drive's scope (per decisions.md D1–D4) is one entry in `FIELD_RULES`, one handler function (`reconcile_card_maturity`), and one walker extension (`apply_top_level_transform`) wired into `walk_and_classify`. The implementation matches the design decisions line-for-line: `Disposition::Transform` reuse (D1), pass-through + map + quarantine four-outcome routing (D2), hard-coded two-arm match without project-local registry (D3), and detail strings (`legacy rename: <from> -> <to>`, `canonical pass-through: <value>`, `unknown maturity value: …`, `maturity expected a string, got …`) matching `reconcile_ac_type`'s house style verbatim (D4). Six new tests cover the four outcomes plus a defensive non-string-shape case, plus the pre-existing `card_scenarios_inner_field_walked` test was correctly updated to assert the second disposition (maturity pass-through) — the AC-10-as-self-tested move the spec explicitly calls out. The walker extension is the only structurally novel piece, and its design (mirror `recurse_inner`'s pattern at top level, run after list-recursion, both independent) is sound and well-commented. The full workspace test suite passes (509/509). The diff is small, well-scoped, well-commented, and does not touch any code outside the registry/handler/walker path. Approve.
