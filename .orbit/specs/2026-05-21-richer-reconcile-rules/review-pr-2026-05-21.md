# Pre-Merge Review

**Date:** 2026-05-21
**Reviewer:** Context-separated agent (fresh session)
**Branch:** main (uncommitted working tree)
**Spec:** 2026-05-21-richer-reconcile-rules
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite (workspace) | PASS | 446 passed across 8 suites |
| Reconcile inline tests | PASS | 39 passed (up from 27 baseline, +12 new) |
| AC-04 parity tests | PASS | 6 passed |
| Clippy (core) | PASS for diff | 5 baseline warnings exist in `verbs.rs`/`locks.rs`/`migrate.rs` on `main`; the diff introduces none |
| AC coverage | 8/8 code-typed + 1 doc-typed | All ACs have evidence |

## AC Coverage Report

| AC | Type | Status | Evidence |
|----|------|--------|----------|
| ac-01 (gate) | code | ✓ | `ac_01_synthesise_id_from_filename_round_trips` (reconcile.rs inline) |
| ac-02 (gate) | code | ✓ | `ac_02_wrap_scalar_ac_round_trips` (reconcile.rs inline) |
| ac-03 | code | ✓ | `ac_03_composition_missing_id_and_scalar_ac_round_trips_idempotently` |
| ac-04 (gate) | code | ✓ | 6 parity tests (`ac_04_*` JSON `next_step` set/null + human breadcrumb on both envelopes) |
| ac-05 | code | ✓ | progress.md captures the finetype migration: 54 rewrote / 0 parse_failed / `orbit verify` clean |
| ac-06 | doc | ✓ | `.orbit/choices/0023-reconcile-as-canonicalise-mode.yaml` updated — Consequences bullet + References entry both shipped |
| ac-07 (gate) | code | ✓ | 6 mechanism-level tests (`ac_07_*`) for Synthesise/WrapListElement variants + pre-walk phase + list-element scope |
| ac-08 (gate) | code | ✓ | 2 tests (`ac_08_synthesise_status_default_open_when_missing` + `ac_08_status_synthesis_composes_with_id_synthesis`) |
| ac-09 (gate) | code | ✓ | `ac_09_criterion_renamed_to_description` |

## Findings

### [INFO] Fixture-path wording in spec is satisfied by inline tests
**Category:** test-gap (deferred — not a real gap)
**Description:** ACs 01/02/03 name on-disk fixture paths under `orbit-state/crates/core/tests/fixtures/reconcile/{missing-id,scalar-ac,missing-id-and-scalar-ac}/spec.yaml`, and ac-07 names a test file at `orbit-state/crates/core/tests/reconcile_shape_extensions.rs`. None of these on-disk artefacts exist.
**Evidence:** `orbit-state/crates/core/tests/` does not exist; no `fixtures/reconcile/` tree. The new tests live inline in `orbit-state/crates/core/src/reconcile.rs:1592-1928` and materialise spec.yaml inputs via `write_spec(&layout, id, body)` inside per-test temp dirs.
**Recommendation:** None required. The inline pattern follows the convention set by spec 2026-05-12-reconcile-mode (predecessor) — every reconcile test uses `write_spec()`, not static fixtures. The tests do round-trip real spec.yaml files on disk through `--reconcile` and re-parse against the canonical `Spec` schema, meeting the spec's intent. ac-07 explicitly licenses this with "or equivalent". Worth a one-line note in the predecessor pattern docs if you want to fossilise the convention, but not blocking.

### [INFO] Choice 0023 anticipation honoured
**Category:** architectural-fit
**Description:** Choice 0023 explicitly anticipated this work — "legacy fields needing value-level handling default to quarantine and a follow-up spec teaches the registry the richer rule shape once a second project demands it". This spec is that follow-up, and the choice file is correctly updated to record both the new transform rules and the registry-shape extensions.
**Evidence:** `.orbit/choices/0023-reconcile-as-canonicalise-mode.yaml:42` (Consequences bullet), `.orbit/choices/0023-reconcile-as-canonicalise-mode.yaml:48` (References entry).
**Recommendation:** None — clean architectural fit.

### [INFO] Two follow-up memos surfaced
**Category:** architectural-fit
**Description:** Two memos exist in the untracked tree capturing the spec's findings:
- `.orbit/memos/2026-05-21-reconcile-dispositions-on-parse-fail.md` — `reconcile_one` drops accumulated dispositions when typed re-parse fails (`reconcile.rs:421-427` per the memo's line ref). Found during the ac-05 finetype dry-run; doesn't block this spec because the broadened rule set produced zero `parse_failed`, but worth a follow-up spec.
- `.orbit/memos/2026-05-21-validate-memo-claims-against-substrate.md` — the source memo's diagnosis ("missing id (~36 specs)") was wrong; reality was "missing status (53 of 54)". Pattern memo about verifying memo claims via dry-run before distill.
- `.orbit/memos/2026-05-21-substrate-first-under-pressure.md` — additional related observation.
**Evidence:** Three `.orbit/memos/2026-05-21-*.md` files untracked.
**Recommendation:** Mention the dispositions-on-parse-fail finding in the eventual commit message so the follow-up surface is discoverable; the memos themselves are the right disposition for both.

### [INFO] Defensive routing for misregistered dispositions
**Category:** robustness
**Description:** `walk_and_classify`, `apply_top_level`, and `apply_inner` all carry explicit match arms routing `Synthesise` / `WrapListElement` variants to Quarantine if they ever reach a path they shouldn't (e.g. a Synthesise rule whose key is somehow present at top-level apply time, or a WrapListElement key that matches a raw mapping key). The "belt-and-braces guard" comments are clear and the behaviour is testable. Good defensive shape.
**Evidence:** `reconcile.rs:718-732` (top-level), `reconcile.rs:906-916` (apply_top_level), `reconcile.rs:980-990` (apply_inner).
**Recommendation:** None — well-designed.

---

## Honest Assessment

This implementation is exemplary. The diff carries 597 net-added lines in `reconcile.rs` (12 new inline tests + 4 new transform rules + 2 registry-shape extensions), 47 in `main.rs` (the breadcrumb wiring, factored into two helpers `parse_failed_breadcrumb` + `push_next_step` that keep both envelopes in lockstep), 195 in `parity.rs` (6 CLI parity tests), and 3 in substrate (choice update + card linkage). Every gate AC has a passing test bearing the exact AC identifier. The single non-code AC (ac-06, `ac_type: doc`) carries the right evidence shape. The non-test AC (ac-05) is closed by a substantive progress.md that captures the full finetype migration with disposition counts and the `orbit verify` clean confirmation.

The mid-implementation scope broadening (ac-08 + ac-09 added after the ac-05 validation dry-run inverted the source memo's diagnosis) was the right move and was handled per `/orb:implement`'s escalation discipline. The two added rules are mechanically symmetric to the original pair and ride on the same registry-shape extensions, so the broadening cost the implementation almost nothing in surface area.

The fixture-path wording in ac-01/02/03/07 versus the actual inline-test reality is a literal-text deviation, not a substantive gap — the in-memory `write_spec()` helper still writes real spec.yaml files to disk inside a temp tree, runs the production reconcile path against them, and re-parses against the canonical schema. The convention matches the predecessor spec 2026-05-12-reconcile-mode and ac-07's "or equivalent" clause explicitly licenses it.

The diff is uncommitted in the working tree. The natural next step is a single commit that closes the spec drive — the spec is already in `closed` state, so the close machinery has already run; the working tree just needs to land in git history.
