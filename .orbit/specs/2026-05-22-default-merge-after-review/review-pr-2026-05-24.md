# Pre-Merge Review

**Date:** 2026-05-24
**Reviewer:** Context-separated agent (fresh session)
**Branch:** main (working tree, pre-commit)
**Spec:** 2026-05-22-default-merge-after-review
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite | N/A | Spec is a doc-only edit to `plugins/orb/skills/drive/SKILL.md` and `.orbit/cards/0014-default-merge-after-review.yaml`; no code paths touched, no tests required |
| AC coverage | 8/9 | See report below — ac-09 is `ac_type: observation` (deferred by design) |

## AC Coverage Report

Type-keyed evidence expectations applied: ac-01..ac-08 are `ac_type: doc` (closed by SKILL.md prose); ac-09 is `ac_type: observation` (closed by post-cutover data captured across 5 future drives — legitimately deferred per `.orbit/METHOD.md` close-time bands).

| AC | Type | Status | Evidence |
|----|------|--------|----------|
| ac-01 | doc | PASS | SKILL.md L573-584 names `gh pr merge --auto` as step 5, explicitly guarded by `if autonomy == full` (L575). Section header (L535-538) orders the six steps and confirms REQUEST_CHANGES/BLOCK never reach §Completion. |
| ac-02 | doc | PASS | SKILL.md L575-578 scopes the merge step to `autonomy == full` and explicitly defers to the four-option prompt for guided/supervised. Four-option prompt section (L493-531) is unchanged from main — `git diff` shows no edits to L495-531. |
| ac-03 | doc | PASS | SKILL.md L535-538 scopes §Completion to APPROVE-only; merge step (L573+) sits inside that branch. No `gh pr merge` reference appears anywhere in §NO-GO Handling (L687+). |
| ac-04 | doc | PASS | SKILL.md §Completion (L540-625) enumerates exactly six numbered steps: (1) commit implementation L540-543, (2) propose card updates and commit L544-548, (3) push L550-553, (4) gh pr create L555-571, (5) gh pr merge --auto (full-only) L573-618, (6) spec close L620-625. Step 3 push is surfaced as its own explicit step. |
| ac-05 | doc | PASS | SKILL.md L586-618 enumerates the five-step failure branch: (a) log exit code + stderr L593, (b) `orbit spec note` with canonical reason token L594-607, (c) PR url in close-comment L608, (d) continue to step 6 L609-610, (e) exit status 0 L611-613. All five canonical tokens listed (`auto-merge-disabled`, `branch-protection`, `draft-pr`, `auth-failure`, `network-error`) plus an `unknown` fallback for novel shapes — the addition is a defensive cover, doesn't violate the AC's "e.g." token set. Draft-PR call-out at L615-618 explicitly rules out `gh pr ready` and routes drafts to graceful-degradation. |
| ac-06 | doc | PASS | SKILL.md L627-644 enumerates three close-comment fields (PR url, review verdict, merge state) and names the two reachable merge states (`queued`, `deferred-<reason>`). L638-641 explicitly ties `deferred-<reason>` to the spec-note token from step 5 so audit trails align. L643-644 states the payload is appended to existing close output — no new infrastructure. |
| ac-07 | doc | PASS | SKILL.md L555-568 describes `gh pr view --json number,autoMergeRequest,state` inspection before `gh pr create` and the conditional skip-to-step-5 shape. L564-566 explicitly states §Resumption is unchanged and no new stage value is introduced. Resumption table (L880-886) confirmed unchanged via `git diff` (no edits in that region). |
| ac-08 | doc | PASS | SKILL.md L26 rewrites the `full` row: "All stages run without human interaction. APPROVE auto-merges via `gh pr merge --auto`. Requires ≥3 card scenarios." The exact "Pauses only for PR merge" phrase is gone (verified by grep returning zero matches). L27 (`guided`) and L28 (`supervised`) unchanged in diff. |
| ac-09 | observation | DEFERRED | `ac_type: observation` — closure requires 5 real-world auto-merged drives plus a summary memo. This is post-cutover empirical evidence by design; not flagged as missing test evidence per `.orbit/METHOD.md` evidence-band table. The AC carries `checked: false` correctly in the spec. |

Coverage: 8/8 closeable doc-ACs satisfied (100%); 1 observation AC legitimately deferred.

## Findings

None at MEDIUM or HIGH. Two LOW notes for the author's awareness (not blocking):

### [LOW] ac-05 adds an `unknown` token beyond the spec's "e.g." set
**Category:** test-gap
**Description:** ac-05's verification clause names five canonical tokens by example (`auto-merge-disabled`, `branch-protection`, `draft-pr`, `auth-failure`, `network-error`). The SKILL.md prose at L603-604 introduces a sixth token `unknown` as the catch-all for un-categorised stderr shapes. The AC text says "e.g." which permits the addition, but the spec-note audit trail will surface `merge deferred — unknown` entries that don't pattern-match any of the named tokens.
**Evidence:** `plugins/orb/skills/drive/SKILL.md:603-604`:
```
- `unknown` — the failure does not match a known shape; capture
  the raw stderr in the spec note alongside this token
```
**Recommendation:** Acceptable as-is — the catch-all preserves graceful-degradation when GitHub returns a novel error and the raw stderr is captured alongside the token for forensic value. Worth a memo against ac-09's observation window if `unknown` shows up in the first 5 drives — that's signal for adding a more precise token in a follow-up edit.

### [LOW] ac-05's spec-note quote text differs subtly from the AC's literal
**Category:** environment-mismatch
**Description:** ac-05's literal text says `orbit spec note <spec-id> "merge deferred — <reason>"`. SKILL.md L594 writes that exact form. This is correct — flagging only because the em-dash (U+2014) in the quote is character-sensitive when grepping; both substrate sites use the same em-dash so audit-trail searches will match. No action required.
**Evidence:** `plugins/orb/skills/drive/SKILL.md:594` matches `2026-05-22-default-merge-after-review/spec.yaml` ac-05 description verbatim.
**Recommendation:** None.

---

## Honest Assessment

This is a clean doc-only edit that lands the §Completion contract the spec interview agreed. The SKILL.md changes are precisely scoped — 103 lines added, 13 removed, all within §Completion and the autonomy table. The four-option prompt section, §NO-GO Handling, and §Resumption are untouched, matching ac-02, ac-03, and ac-07's "unchanged from current behaviour" verification clauses. The six-step ordering at L540-625 maps directly onto ac-04's enumerated step list; the resume idempotency check at L555-568 lands ac-07 without introducing a new `drive.yaml` stage value. Card 0014 picks up its `specs: []` → linked-path update in the same change.

No code, no tests, no test gaps to flag — every closeable AC is `ac_type: doc` and every doc AC has a verbatim or near-verbatim home in the SKILL.md prose. The one deferred AC (ac-09) is correctly typed `observation` and will close on post-cutover evidence after the first five auto-merged drives, per the K1 kill-condition contract from the tabletop.

Approve.
