# Pre-Merge Review

**Date:** 2026-05-24
**Reviewer:** Context-separated agent (fresh session)
**Branch:** feat/2026-05-24-port-acceptance-shim
**Spec:** 2026-05-24-port-acceptance-shim
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| `cargo test -p orbit-state-core -p orbit-state-cli -p orbit-state-mcp` | PASS | 524 passed (8 suites, 1.78s) |
| `orbit verify` | PASS | clean — zero drift on substrate |
| `rg --no-heading 'orbit-acceptance\.sh' plugins/orb/skills/` | PASS | 0 hits (AC-09 gate) |
| Shim + dedicated test deleted in single commit (587b1c0) | PASS | AC-10 invariant holds |
| Shell smoke `test-promote-gate-propagation.sh` | PASS | — |
| Shell smoke `test-setup-method.sh` | PASS | — |
| Shell smoke `test-sidecar-layout.sh` | FAIL | pre-existing, see Findings INFO-1 |
| AC coverage | 12/12 | All ACs evidenced; see report below |

## AC Coverage Report

The implementation contract has 12 ACs across `code`, `gate`, and the broader doc/config/decision surface. The parity-test set covers the five new verbs by surface (CLI/MCP) rather than by AC id — common pattern in `orbit-state/crates/{cli,mcp}/tests/parity.rs`. AC↔test mapping below cites verb-level coverage.

| AC | Type | Status | Evidence |
|----|------|--------|----------|
| ac-01 | code (gate) | ✓ | `spec_acs_cli_json_matches_canonical_envelope` + `spec_acs_mcp_envelope_matches_canonical_envelope`; render preserves tab format at `crates/cli/src/main.rs:2067-2073`; missing-id verified live (`spec.acs: not-found`). |
| ac-02 | code | ✓ | `spec_next_ac_cli_json_matches_canonical_envelope` + MCP mirror; `next_unblocked` at `crates/core/src/verbs.rs:2545-2556`. |
| ac-03 | code | ✓ | `spec_blocking_gate_cli_json_matches_canonical_envelope` + MCP mirror; `first_blocking_gate` at `verbs.rs:2560-2562`. |
| ac-04 | code | ✓ | `spec_has_unchecked_cli_true_emits_envelope_and_exits_zero` + `..._false_emits_envelope_and_exits_one` + MCP mirrors; exit-code-as-data preserved in `main.rs:723`. |
| ac-05 | code | ✓ | `spec_check_cli_json_matches_canonical_envelope` + `..._missing_ac_returns_not_found` + `..._already_checked_returns_conflict` + MCP mirrors; `spec_check`/`spec_uncheck` are first-class core verbs at `verbs.rs:2620-2631`; CLI `--ac-check`/`--ac-uncheck` reroute at `main.rs:1106-1142`; live test confirms reroute (error envelope's `verb` field is `spec.check`, not `spec.update`). |
| ac-06 | code (gate) | ✓ | Gate-axis helpers (`next_unblocked`, `first_blocking_gate`) and raw-axis helper (`any_unchecked`) defined separately at `verbs.rs:2542-2569`. Close-time pre-flight at `verbs.rs:2133-2143` still filters on `ac_type.blocks_close()` — no shared-source claim. |
| ac-07 | code (gate) | ✓ | 8 CLI + 7 MCP parity tests added (15 total) against shared fixtures `populate_spec_acs_mixed_fixture` + `populate_spec_acs_all_checked_fixture`. |
| ac-08 | code | ✓ | One-shot path chosen (verbs + SKILL.md rewrites + shim deletion in two commits). AC-10's no-orphaned-wrapper invariant holds. |
| ac-09 | code (gate) | ✓ | `rg --no-heading 'orbit-acceptance\.sh' plugins/orb/skills/` returns 0. 31 call sites rewritten across 5 SKILL.md files. |
| ac-10 | code | ✓ | `git log --diff-filter=D` confirms commit 587b1c0 deletes both `orbit-acceptance.sh` and `tests/test-gate-ac-verification.sh`. |
| ac-11 | doc | ✓ | `.orbit/choices/0020-shell-scripts-to-rust-verbs.yaml:55` — row marked **MIGRATED** with spec id cited; promote.sh / setup-method.sh rows untouched. |
| ac-12 | code | ✓ | cargo test 524 passing; `orbit verify` clean; `CHANGELOG.md:5-32` enumerates the five verbs and shim removal under `[0.4.34]`; version bumped in `orbit-state/Cargo.toml:10`. |

Coverage: 12/12 (100%).

## Findings

### [INFO-1] Pre-existing failure in `test-sidecar-layout.sh` — unrelated to this PR
**Category:** test-gap
**Description:** The smoke test `plugins/orb/scripts/tests/test-sidecar-layout.sh` fails at step (a) with `FAIL (a): expected flat spec at /tmp/.../.orbit/specs/2026-05-24-sidecar-smoke.yaml, missing`. The test expects flat-shape `.orbit/specs/<id>.yaml` but `promote.sh` produces folder-shape `.orbit/specs/<id>/spec.yaml` since commit `e1b5465 fix(promote): use folder-sidecar spec path`. The test was authored under spec 2026-05-09-drive-rally-sidecar-layout (commit 0830465) and never updated for the brownfield folder-layout migration (spec 2026-05-24-brownfield-spec-migration, commit 1cde25c).
**Evidence:**
- `git log --oneline -- plugins/orb/scripts/promote.sh` shows `e1b5465 fix(promote): use folder-sidecar spec path`.
- Reproduced manually: `promote.sh --root $TMP` produces `$TMP/.orbit/specs/<id>/spec.yaml`, not `$TMP/.orbit/specs/<id>.yaml`.
- `git diff main...HEAD` shows neither `promote.sh` nor `test-sidecar-layout.sh` touched in this PR.
**Recommendation:** Out of scope for this spec. The test needs updating to match folder-shape, or `promote.sh` needs a flat-shape option — that decision belongs with the brownfield-migration follow-up, not here. Mentioned so the next reviewer doesn't chase it.

### [INFO-2] AC-08 wrapper-skip path taken — one-shot landing
**Category:** content-signal
**Description:** The implementer chose AC-08's authorised one-shot path: verbs + parity tests + SKILL.md rewrites + shim deletion landed across two commits (`5500260` core port, `587b1c0` decommission) rather than the wrapper-first variant. Spec explicitly authorised this. Both deletions in `587b1c0` keep AC-10's invariant intact.
**Evidence:** `git log main..HEAD --oneline` shows the two-commit sequence; deletions consolidated in 587b1c0.
**Recommendation:** None — informational.

### [INFO-3] `spec_uncheck` lacks dedicated parity test (structural symmetry only)
**Category:** test-gap
**Description:** AC-05 requires `spec_uncheck` to ship as a symmetric core verb. It does (`verbs.rs:2627-2631`), and shares `flip_ac_checked` with `spec_check` so the read-mutate-write + idempotency logic is identical. The parity suite has explicit tests for `spec_check` (happy / missing-ac / already-checked) plus MCP mirrors, but no direct `spec_uncheck_*` parity test — the uncheck path is exercised only structurally.
**Evidence:** `grep spec_uncheck orbit-state/crates/{cli,mcp}/tests/parity.rs` returns no test functions.
**Recommendation:** Non-blocking. The shared mutator makes asymmetric breakage unlikely. A symmetric `spec_uncheck_cli_already_unchecked_returns_conflict` would close the gap cheaply if a follow-up touches this code.

---

## Honest Assessment

This is a clean port. All twelve ACs are evidenced — the five new verbs (`acs`, `next-ac`, `blocking-gate`, `has-unchecked`, `check` plus the symmetric `uncheck`) ship with byte-faithful CLI rendering and CLI/MCP parity coverage; the gate-axis helpers are properly split from the close-time taxonomy-axis pre-flight per AC-06; the shim and its dedicated test delete in the same commit as the last SKILL.md rewrite (AC-10 invariant); the CHANGELOG, choice 0020, version bump, and `orbit verify` clean are all in place. The pre-existing `test-sidecar-layout.sh` failure is unrelated to this PR (promote.sh switched to folder-shape under a separate spec without updating the test). The only thin spot is dedicated `spec_uncheck` parity coverage, which the shared mutator makes structurally robust. Ready to merge.
