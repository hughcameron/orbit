# Pre-Merge Review

**Date:** 2026-05-09
**Reviewer:** Context-separated agent (fresh session)
**Branch:** design-session-user-language
**Spec:** 2026-05-09-design-session-user-language
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| `orbit verify` | PASS | clean |
| `orbit-state` cargo tests | PASS | 136 passed across 8 suites |
| `test-promote-gate-propagation.sh` | PASS | gate propagation verified end-to-end |
| `test-gate-ac-verification.sh` | PASS | gate-AC rules behave as expected |
| AC coverage | 0/9 (doc-only spec) | see report below |

## AC Coverage Report

| AC | Status | Test(s) |
|----|--------|---------|
| ac-01 | n/a (gate, doc) | NO TEST FOUND — design-space classification documented at `plugins/orb/skills/design/SKILL.md:69-85` |
| ac-02 | n/a (gate, doc) | NO TEST FOUND — closed-mode design-note path at `plugins/orb/skills/design/SKILL.md:87-127`; spec input acceptance at `plugins/orb/skills/spec/SKILL.md:18-26` |
| ac-03 | n/a (gate, doc) | NO TEST FOUND — implementation-question filter at `plugins/orb/skills/design/SKILL.md:162-175` |
| ac-04 | n/a (gate, doc) | NO TEST FOUND — top-of-file user-voice paragraph slot in interview template at `plugins/orb/skills/design/SKILL.md:227-229`; agent-drafts pattern at `plugins/orb/skills/design/SKILL.md:138-144` |
| ac-05 | n/a (gate, doc) | NO TEST FOUND — wires landed in named files (verified by inspection of design SKILL.md and spec SKILL.md diffs) |
| ac-06 | n/a (non-gate, doc) | NO TEST FOUND — intent-contract citation rule at `plugins/orb/skills/spec/SKILL.md:26` and `plugins/orb/skills/spec-architect/SKILL.md:18` |
| ac-07 | n/a (non-gate, doc) | NO TEST FOUND — mode-switch trigger after repeated rejection at `plugins/orb/skills/design/SKILL.md:179` |
| ac-08 | n/a (non-gate, doc) | NO TEST FOUND — multiple-choice-as-smell guidance at `plugins/orb/skills/design/SKILL.md:177` |
| ac-09 | n/a (non-gate, doc) | NO TEST FOUND — clean exit / "no interview is normal" at `plugins/orb/skills/design/SKILL.md:85, 121-127` |

This spec is documentation-only — three SKILL.md files (`plugins/orb/skills/design/SKILL.md`, `plugins/orb/skills/spec/SKILL.md`, `plugins/orb/skills/spec-architect/SKILL.md`) edited to encode the three-mode design-space gate, the implementation-question filter, the user-voice paragraph slot, and the spec-side citation rule. Every AC is verified by inspection of the SKILL prose; none warrant a test by design. The shell test suite that protects orbit-state's gate-AC tooling is unaffected and continues to pass.

## Findings

### [LOW] Closed-mode example cites a choice/specs configuration that doesn't yet exist
**Category:** doc-drift
**Description:** The closed-mode classification example in design SKILL.md reads `"Card 0031: closed. Choice 0012 pins the intent-vs-means distinction; prior specs apply it. Producing a design note rather than running an interview."` Card 0031 is the very card this spec implements — at the time of merge it is *not* in closed mode (its `specs:` array currently contains only the in-flight spec, and the spec is not yet shipped). The example will read as accurate after this spec ships and any future spec under 0031 runs through `/orb:design`, but on first-pass reading by a new agent the example may parse as load-bearing prose rather than illustrative.
**Evidence:** `plugins/orb/skills/design/SKILL.md:83`; `.orbit/cards/0031-design-session-user-language.yaml:53` shows `specs: []`; the spec metadata `2026-05-09-design-session-user-language.yaml` has `status: open`.
**Recommendation:** No action required for this PR. The example is forward-looking and becomes literally true once this spec ships and a follow-up design session runs against 0031. Optional: replace the card-specific example with a generic placeholder (`Card NNNN: closed. Choice NNNN pins …`) on a future doc-cleanup pass. Not blocking.

### [INFO] ac-07 rejection-detection heuristic is concretised in skill prose
**Category:** assumption
**Description:** The /orb:review-spec round flagged ac-07 as leaving the rejection-detection mechanism unspecified. The implementation pinned a concrete heuristic in `plugins/orb/skills/design/SKILL.md:179` — explicit author phrasing (`"that's an implementation detail"`, `"ask the implementing agent"`, `"I'd need codebase context"`, `"that's a code question"`) is the rejection signal. This resolves the spec-review concern: the AC verifies the skill *documents* the trigger, and the documented trigger now names a detection rule rather than leaving it to runtime invention.
**Evidence:** `plugins/orb/skills/design/SKILL.md:179` — explicit phrasing list and the two-rejection counter rule.
**Recommendation:** No action.

### [INFO] ac-06 intent-contract citation is wired in two places
**Category:** redundancy-by-design
**Description:** ac-06 requires `/orb:spec` to cite the user-voice paragraph as the intent contract. The implementation lands the rule in *both* `plugins/orb/skills/spec/SKILL.md:26` (as a top-level instruction in the user-facing skill) and `plugins/orb/skills/spec-architect/SKILL.md:18` (as a top-level instruction in the spec-architect persona invoked from the spec skill). Belt-and-braces. The two instructions don't disagree, and the persona is the load-bearing extraction site, so the duplication is defensible.
**Evidence:** identical-intent prose at `spec/SKILL.md:26` and `spec-architect/SKILL.md:18`.
**Recommendation:** No action. Acceptable redundancy — both files are read at different points in the pipeline and the rule needs to survive both.

---

## Honest Assessment

The diff is small (218 insertions, 18 deletions across three SKILL.md files plus the spec metadata, drive state, and the prior review-spec record), surgical, and respects choice 0012 (intent-not-means) and choice 0019 (cards declare framework wires in scenarios). All nine ACs are documentation-only edits to skill prose; the implementation-question filter is named explicitly as the gate every question passes through; the closed-mode path produces a `design-note.md` artefact and is named as a normal terminal outcome rather than a failure mode; the user-voice paragraph slot lands in both the interview template and the design-note template, with the agent-drafts-then-author-edits pattern made explicit; `/orb:spec` and `/orb:spec-architect` both carry the intent-contract citation rule. The keyword scan against `.orbit/choices/` matched 0012 and 0019 — the implementation is operationalising 0012's "would the author need codebase context to answer this?" filter and following 0019's wires-in-scenarios pattern. No violations.

The /orb:review-spec round flagged two LOWs: ac-05 as a roll-up of ac-01..ac-04 (intentional belt-and-braces under choice 0019 — kept) and ac-07's rejection-detection heuristic as unspecified (resolved in implementation by naming the rejection phrases at `design/SKILL.md:179`). Both spec-review concerns are addressed or accepted.

No test gaps because there is no code change. The spec is a documentation amendment to skill prose, verified by inspection plus `orbit verify` clean and the unaffected shell-test plus 136-cargo-test suite. The only finding worth surfacing is a forward-looking example in the design SKILL that names card 0031 in closed mode before 0031 has shipped a spec — non-blocking, becomes literally true after merge.

The drive state for this spec (`drive.yaml`) shows `iteration: 1, stage: implement, review_spec_cycle: 1, review_pr_cycle: 0` — consistent with this being the first review-pr pass. The verdict APPROVE means drive will advance to merge on this iteration without a re-review cycle.
