# Pre-Merge Review

**Date:** 2026-05-24
**Reviewer:** Context-separated agent (fresh session)
**Branch:** rally/workflow-conformance
**Spec:** 2026-05-24-workflow-conformance
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite | PASS | 504/504 tests across 8 suites (`cargo test --workspace`) |
| AC coverage | 12/12 | See report below |
| Live smoke | PASS | `orbit --json audit conformance` returns a well-formed envelope (4 stale-memo findings against this dotted repo; aggregated drift/topology populated; pin status surfaced) |

## AC Coverage Report

PR scope on this branch is ACs 09–12 (the `undotted_substrate` finding family). ACs 01–08 shipped earlier in commit `8227b54` and are covered by pre-existing tests (`conformance_clean_repo_zero_findings`, `conformance_idempotent_two_invocations_byte_equal`, the memo-staleness/card-state/pin-state/byte-drift unit tests in `verbs.rs`). The drive marked all twelve `checked: true` to consolidate the AC ledger now that the layout-dominance branch makes the family complete.

| AC | Status | Evidence |
|----|--------|----------|
| ac-01 | PASS | Earlier work — envelope shape exercised by `conformance_*` unit tests + clean/wrapped fixture parity tests; live smoke confirms the per-finding fields (subject/state/severity/evidence/remediation.verb) appear |
| ac-02 | PASS | `audit_conformance_at` (verbs.rs:3949–3950) calls `audit_drift` + `audit_topology` and returns them under `aggregated` on every branch; live smoke confirms |
| ac-03 | PASS | `memo_staleness_findings` exercised by earlier unit tests; live smoke shows four `medium/stale` memo findings with `/orb:distill <path>` remediation |
| ac-04 | PASS | `card_state_findings` covered by earlier unit tests; emission shape (subsystem `cards`, state `ready_for_tabletop`, remediation `/orb:tabletop <numeric-id>`) verified at verbs.rs:4070–4078 |
| ac-05 | PASS | `canonical_file_findings` covered by earlier unit tests; suppression behaviour preserved through the refactor (test `audit_conformance_suppresses_canonical_files_missing_on_wrapped_undotted` still passes) |
| ac-06 | PASS | `pin_dominates` branch (verbs.rs:3990) gates `canonical_file_findings`; pin-state coverage from earlier work |
| ac-07 | PASS | `conformance_clean_repo_zero_findings` (verbs.rs:10981) + `conformance_idempotent_two_invocations_byte_equal` (verbs.rs:10994) |
| ac-08 | PASS | Doc/behavioural AC — single remediation verb per finding, zero-finding case returns empty `findings[]` (verified by ac-07 tests) |
| ac-09 | PASS | `undotted_finding_fires_when_orbit_present_and_dotted_absent`, `undotted_finding_suppressed_when_canonical_substrate_present`, `undotted_finding_absent_on_greenfield`, `undotted_finding_absent_when_only_orbit_dir_with_no_substrate_subdirs` (verbs.rs:12501–12548) — full predicate matrix |
| ac-10 | PASS | `undotted_finding_fires_when_orbit_present_and_dotted_absent` asserts severity=high, subsystem=setup, state=undotted_substrate, subject=`orbit/`, remediation.verb=`orbit setup`, evidence map carrying `cards_count` (with extension to the other three subdirs via the loop at verbs.rs:4257–4272) |
| ac-11 | PASS | `audit_conformance_emits_undotted_substrate_and_suppresses_others_on_wrapped_undotted` (verbs.rs:12555–12578) asserts exactly one finding and the absence of `missing/ready_for_tabletop/stale/pin_behind/pin_ahead/decisions_md_unmigrated` states; routine-findings + drift/topology continuation visible structurally (routine emission at verbs.rs:4015 lives outside the layout-dominance `if/else`) |
| ac-12 | PASS | `audit_conformance_cli_wrapped_undotted_envelope` (cli/tests/parity.rs:1685–1721) and `audit_conformance_mcp_wrapped_undotted_envelope` (mcp/tests/parity.rs:838–855) each byte-compare against `common::expected_envelope_for_audit_conformance_wrapped_undotted` — single library reference exercised on both surfaces |

Note on test-naming convention: this codebase uses descriptive test names with `// per spec <id> ac-NN` comments rather than the bare `ac<NN>` prefix the review skill's default regex looks for. The AC-to-test mapping above was made by reading test intent against AC text. Both naming styles satisfy the contract; the descriptive-name style is consistent with the rest of `verbs.rs`.

## Layout-dominance refactor (sanity check)

The diff reworks the suppression logic at verbs.rs:3955–4006. The previous PR (spec 2026-05-24-setup-is-orbit-state-aware) had introduced a `layout_non_canonical` boolean gating `canonical_file_findings` only. This PR replaces that with `layout_dominates` (computed from `undotted_substrate_finding.is_some()`) and widens the suppression to the full set of `.orbit/`-dependent families (card-state, memo-staleness, pin-state, canonical-files, decisions-md-unmigrated) — matching decisions.md §6 verbatim. Routine-findings + aggregated drift/topology remain on every branch.

The earlier `audit_conformance_suppresses_canonical_files_missing_on_wrapped_undotted` test still passes — confirming the prior-spec contract is preserved while the wider suppression surface lands.

## Decisions alignment

Walked each of the six decisions in `decisions.md` against the implementation:

- §1 (predicate B — any of four subdirs + negative guard): verbs.rs:4253–4256 + 4243–4246. Match.
- §2 (emission-time skip mirroring `pin_dominates`): verbs.rs:3965–3994. Match.
- §3 (subsystem `setup`, subject `orbit/`, state slug carries discriminator): verbs.rs:4273–4276. Match.
- §4 (HIGH unconditional + evidence counts): verbs.rs:4274 + 4257–4272. Match.
- §5 (state slug `undotted_substrate`): verbs.rs:4276. Match.
- §6 (suppress canonical-files-missing + card-state + memo-staleness + pin-state, keep routine + aggregated drift/topology): verbs.rs:3975–4006 + 4008–4015. Match.

## Choices scan

Keyword-scanned `.orbit/choices/` for `conformance` / `undotted` / `layout_dominates`. Only `0023-reconcile-as-canonicalise-mode.yaml` and `0025-topology-substrate-folder.yaml` reference conformance, and neither constrains the layout-dominance design. No conflicts.

## Findings

None.

---

## Honest Assessment

Tight, on-decisions, well-tested. The new finding is one helper (`undotted_substrate_finding`) and one branch in `audit_conformance_at`; both follow the existing `pin_dominates` shape exactly, so a future reader carries no extra concepts. Evidence is precise (per-subdir counts in a YAML mapping), severity is unambiguous (HIGH), remediation verb is the single `orbit setup` the decisions doc committed to. The layout-dominance refactor cleanly subsumes the earlier `layout_non_canonical` partial-suppression that shipped under the sister spec — the prior test still passes, so the contract is preserved. Parity coverage is tight: one library reference, two surfaces, byte equality. The full 504-test workspace passes. Ship it.
