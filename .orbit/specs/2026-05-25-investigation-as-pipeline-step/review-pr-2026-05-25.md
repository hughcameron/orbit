# Pre-Merge Review

**Date:** 2026-05-25
**Reviewer:** Context-separated agent (fresh session)
**Branch:** investigation-as-pipeline-step
**Spec:** 2026-05-25-investigation-as-pipeline-step
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite (cargo test --release) | PASS | 554 passed, 0 failed, 8 suites |
| AC coverage | 7/7 gate ACs evidenced via prose-grep + smoke (ac-08 deferred) | See report below |
| Hook smoke | PASS | substrate-skip silent; off-pipeline warns with bypass-log guidance |
| Choice keyword scan | clean | choice 0029 is the new MADR; implementation respects it |

## AC Coverage Report

| AC | ac_type | Status | Evidence shape |
|----|---------|--------|----------------|
| ac-01 | doc | check | `.orbit/choices/0029-pipeline-orchestrates-investigation.yaml` filed, status accepted, cites 610/0, names per-stage scope-derivation table, bypass shape, skill-tool return contract, args-drop guard, hook decision |
| ac-02 | code (default) | check | `plugins/orb/skills/implement/SKILL.md:147-191` adds per-AC structural step; imperative prose ("Invoke /orb:code-investigate ... narrow mode"); names agent-typed scope sources (current AC description + tabletop.md Adjacent-code + cards' references[]); args-drop guard via `orbit spec note` BEFORE Skill call; bypass via AUQ with `orbit spec note "investigation bypass [ac-NN]: ..."` |
| ac-03 | code (default) | check | `plugins/orb/skills/tabletop/SKILL.md:177-193` Q8 section gains broad-mode orchestrated step; scope from cluster cards' references[]; args-drop guard via labelled memory (`orbit memory remember tabletop-investigation-scope-...`); bypass shape names two-option AUQ |
| ac-04 | code (default) | check | `plugins/orb/skills/review-pr/SKILL.md:55-75` Phase 2 entry adds narrow-mode orchestrated step; names both gh-first and git-diff-fallback resolution paths; explicit base-resolution-failure bypass; args-drop guard via `orbit spec note` |
| ac-05 | code (default) | check | `plugins/orb/skills/researcher/SKILL.md:20-36` gains broad-mode orchestrated step; scope from topic argument; explicit no-argument bypass path; args-drop guard via labelled memory |
| ac-06 | doc | check | `plugins/orb/skills/drive/SKILL.md:387-393` and `plugins/orb/skills/rally/SKILL.md:513-518` each carry the one-line "fires at the routed stage" acknowledgement citing choice 0029 |
| ac-07 | config | check | `plugins/orb/hooks/code-investigate-nudge.sh` warning text sharpened (lines 52-72) — names off-pipeline framing, points at both bypass-log paths; `plugins/orb/hooks/hooks.json` PreToolUse Edit|Write matcher unchanged; smoke confirms substrate skip + off-pipeline warn |
| ac-08 | observation | deferred | Pre-ship per-repo baseline filed at `.orbit/memos/2026-05-25-investigation-orchestration-preship-baseline.md` (commit 05ebec1, precedes the SKILL.md commit 5125572). Post-ship +4w audit is deferred per ac-taxonomy observation band — does not block close |

Coverage: 7/7 gate ACs have evidence; ac-08 legitimately deferred.

## Findings

### [LOW] Smoke-marker verification clauses (ac-02..ac-05 part 2) were not exercised in this cycle
**Category:** test-gap
**Description:** ac-02..ac-05 each carry a two-part verification: (1) prose-grep on the SKILL.md, and (2) smoke-marker — invoke the modified pipeline skill in a fresh session, then assert that `notes.jsonl` (spec-bound stages) or labelled memory (session-bound stages) carries the intended-scope line written BEFORE the Skill call AND that `.orbit/.code-investigate-recent` carries matching marker entries. The prose-grep half is satisfied for all four. The smoke-marker half is unexercised — `notes.jsonl` for this spec carries only three pre-implementation reframe entries; `.orbit/.code-investigate-recent` does not exist in the repo; `orbit memory list` shows no `tabletop-investigation-scope-*` or `researcher-investigation-scope-*` entries from this PR window.
**Evidence:**
- `wc -l .orbit/specs/2026-05-25-investigation-as-pipeline-step/notes.jsonl` → 3 lines, all reframe notes pre-dating implementation
- `ls .orbit/.code-investigate-recent` → No such file or directory
- `grep -c "investigation scope" .orbit/specs/2026-05-25-investigation-as-pipeline-step/notes.jsonl` → 0
- `grep -c "investigation bypass" .orbit/specs/.../notes.jsonl` → 0
**Recommendation:** The smoke-marker clauses fire on the *next* invocation of each pipeline skill — they are by design forward-looking, not in-PR. ac-08's +4w audit collects this evidence at population scale. The right disposition is to treat the smoke-marker clauses as deferred verification (their natural home is post-merge), not as in-PR gates. No change required for merge. Log a session memory at first post-merge invocation of `/orb:implement` so the smoke is captured on the next spec drive.

### [LOW] ac-02..ac-05 default to `ac_type: code` but are doc-shaped
**Category:** test-gap
**Description:** Four ACs (ac-02 through ac-05) omit `ac_type` and therefore default to `code` per the ac-taxonomy. Their deliverable is SKILL.md prose — `doc`-shaped. The reviewer applies type-keyed evidence expectations from each AC's description text (per `review-pr/SKILL.md` Phase 2 guidance) and treats prose-grep + smoke as the evidence shape, but the typed band would have surfaced these as doc-band cleanly. The test prefix `pipinv` carried no rust tests as a result, which is correct given the doc-shape but creates a false-positive risk in any AC-coverage tool that doesn't read description text.
**Evidence:** `grep -n "ac_type:" .orbit/specs/2026-05-25-investigation-as-pipeline-step/spec.yaml` returns only four lines (ac-01 doc, ac-06 doc, ac-07 config, ac-08 observation). ac-02..ac-05 inherit default `code`.
**Recommendation:** Non-blocking — the description text makes the doc-shape unambiguous for any human reviewer. Capture as a memo for future spec authoring: prefer explicit `ac_type: doc` on SKILL.md-prose ACs even when the deliverable also gates downstream behaviour, so review-pr's type-keyed walk hits the right evidence shape without reading description text.

---

## Honest Assessment

The PR ships the orchestrate mechanism (choice 0029) across all four pipeline skills (`implement` per-AC, `tabletop` Q8, `review-pr` Phase 2, `researcher` thread entry), plus the drive/rally acknowledgements, plus the hook warning-text sharpening, plus the ac-08 pre-ship baseline. Prose checks pass for every gate AC. The test suite (554/554) is green. The hook still loads and behaves correctly per the smoke (substrate skip, off-pipeline warn, both bypass paths cited). Choice 0029 is internally consistent with the SKILL.md prose — same scope-derivation table, same args-drop guard, same bypass-log paths, same skill-tool return contract.

The two findings are both LOW. The smoke-marker verification clauses for ac-02..ac-05 are by design forward-looking — they fire on the next invocation of the modified pipeline skills, not within this PR. That is the right shape (a SKILL.md-prose change is verified by *running* the modified skill, which happens post-merge), so the deferred verification is correctly aligned with the +4w audit (ac-08). The second finding is a substrate-hygiene note about explicit `ac_type` annotation on doc-shaped ACs — captured for future spec authoring, no impact on this merge.

The implementation respects the cycle-2 and cycle-3 reframes locked in `notes.jsonl`: agent-typed scope, smoke-marker clauses present in verification, args-drop guard wired through every site, hook-retain-as-backstop, dead-substrate refs (progress.md / session-card) avoided. The drive deviation from synthetic-BLOCK at cycle-3 (monotonic convergence pattern) is documented in `drive.yaml:9-15` with a clean three-question-test justification — that's also fine.

Recommend APPROVE.
